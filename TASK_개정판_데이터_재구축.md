# 작업 지시서 (개정판): 데이터 재구축

> **이 문서는 기존 지시서 `TASK_뉴스시점정합성_및_베이스라인.md`를 완전히 대체한다.**
> 기존 문서는 삭제되었으며, 그 안의 Task 0~4 순서는 더 이상 유효하지 않다.
> (기존 Task 0은 완료되었고, 기존 Task 2를 지금 실행하면 표본이 더 줄어들어 순서가 잘못되었다.)

## Task 0 결과 요약 및 판단

| 항목 | 수치 |
|---|---|
| 뉴스 행 수 | 10,618 |
| **고유 (ticker, date) 조합** | **244** ← 실질 표본 수 |
| 조합당 기사 수 | 중앙값 49 (크롤러 50건 상한에 밀착) |
| SK하이닉스 라벨링 뉴스 | 0건 (※ 자리표시자 종목, 분석 대상 아님 — Task A-2 참조) |
| 완전 중복 제목 | 1,148건 (10.8%) |
| KoBERT macro F1 | 0.1473 |
| char TF-IDF macro F1 | 0.1498 |

**판단**: 9200만 파라미터 모델과 선형 bag-of-words가 동일 점수를 냈다.
모델 문제가 아니라 데이터 문제이며, 병목은 **표본 수 244개**다.
5-class 분류에 244 샘플, test는 약 37일치. 어떤 모델도 여기서 더 나올 수 없다.

**따라서 기존 지시서의 Task 2(윈도우 재정렬)를 지금 실행하면 안 된다.**
244개를 더 줄이는 방향이기 때문이다. 표본 확보가 선행되어야 한다.

---

## 개정된 착수 순서

```
Task A. 원인 확정 진단 (30분, 코드 변경 없음)   → 보고 후 정지
Task B. 수집기 교체 + 시각 확보                  → 보고 후 정지
Task C. 전면 재수집 (표본 확장)                  → 보고 후 정지
Task D. 윈도우 재정렬 (기존 Task 2)
Task E. 초과수익률 라벨링 (기존 Task 3)
Task F. 조합 비교 실험 (기존 Task 4)
```

기존 지시서의 Task 1은 Task B에 흡수되었고, 내용이 바뀌었다.

---

## Task A — 원인 확정 진단 (즉시, 코드 변경 없음)

Task 0에서 나온 분포 이동과 SK하이닉스 0건의 원인을 확정한다.
**SQL 조회와 출력만 한다. 수정하지 않는다.**

### A-1. 분포 이동의 원인: 수집 소스 혼재 가설 검증

**가설**: 현재 `daily_news`에는 표본 추출 규칙이 다른 두 소스가 섞여 있다.

- `NaverNewsCrawl.py`: 등락률 임계값 초과일만 수집 → 라벨이 ±1, ±2에 몰림
- `NaverNews.py`: 매일 당일 뉴스 수집 → 라벨이 0에 몰림

시기별로 두 소스의 비중이 달라지면, 시계열 split에서 라벨 분포가 그대로 이동한다.
train 50.4%가 중립(0)인 것은 변동성 필터 수집만으로는 설명되지 않는다.

**검증 쿼리** (아래를 모두 출력):

1. `daily_news`를 `daily_stock_prices`와 JOIN하여, 각 행의 해당일 `ABS(change_rate)` 구간별 행 수
   - `< 1%`, `1~2%`, `2~3%`, `>= 3%` 구간으로 집계
   - `< 2%` 구간의 행은 변동성 크롤러가 수집할 수 없는 행 → `NaverNews.py` 유래로 추정
2. 위 구간 분류를 **월 단위(`date_trunc('month', date)`)로 교차 집계**
   - 저변동 구간 비율이 시기에 따라 크게 달라지는지 확인
   - train/val/test 경계(2026-02-09, 2026-05-14)를 기준으로 비율 비교
3. `daily_news.id`가 auto-increment라면 id 구간과 date의 관계를 산점도 대신 요약 통계로 출력
   - 적재 순서로 소스를 역추적할 수 있는지 확인

**판정 기준**: 저변동(<2%) 행의 비율이 split 간에 뚜렷이 다르면 가설 확정.
이 경우 분포 이동은 시장 레짐 변화가 아니라 **수집 설계 결함**이며, Task C의 균일 수집으로 해결된다.

### A-2. 다종목 파이프라인 무결성 점검

SK하이닉스(000660)는 확장성 검증용으로 등록해둔 종목이며, **현재 분석 대상이 아니다.**
그러나 "코드에 등록되어 있는데 라벨링된 뉴스가 0건"이라는 사실 자체는 점검 대상이다.
Task C에서 종목을 늘릴 때 **동일한 버그로 신규 종목이 조용히 누락될 수 있기 때문**이다.

즉 이 점검의 목적은 SK하이닉스 데이터를 복구하는 것이 아니라,
**다종목 순회 파이프라인이 실제로 동작하는지 확인**하는 것이다.

다음을 순서대로 확인해 어느 단계에서 끊겼는지 특정한다.

1. `SELECT ticker, COUNT(*) FROM daily_news GROUP BY ticker` — 000660 행이 아예 없는지, 있는데 라벨만 NULL인지
2. `SELECT COUNT(*) FROM daily_stock_prices WHERE ticker='000660'` — 주가 데이터는 있는지
3. 행은 있고 라벨만 NULL이라면 `sentiment_score_label.py`의 JOIN 조건 확인 (ticker 타입 불일치, 공백, 선행 0 손실 등)
4. 행 자체가 없다면 종목 순회 로직 확인 — 리스트에 등록만 되고 실제로 순회되지 않는지, 예외가 조용히 삼켜지는지

**결과는 "원인 특정"까지만 하고 수정하지 않는다.** 수정은 Task B의 신규 크롤러 설계에 반영한다.
특히 4번(순회 누락)으로 판명되면, 신규 크롤러는 **종목별 수집 건수를 반드시 요약 출력**하도록 만들어
0건 종목이 눈에 띄지 않고 지나가지 않게 해야 한다.

### A-3. 중복 제목 실태

- 완전 중복 제목 1,148건이 (a) 같은 기사의 재적재인지 (b) 통신사 기사 전재인지 구분
- 중복 그룹별 `(ticker, date)` 분포 출력 — 같은 날 안에서의 중복인지, 여러 날에 걸친 중복인지

### 완료 조건

위 세 항목 결과 출력 후 **정지**. 수정 작업 없음.

---

## Task B — 수집기 교체 + 발행 시각 확보

**결정 사항: 기존 네이버 뉴스 검색 크롤러를 수리하지 않고, 네이버 금융 종목뉴스 페이지 크롤러로 교체한다.**

이유:
- 날짜/시각이 테이블 컬럼으로 분리되어 있어 파싱이 안정적 (`오전 8:12` 정규식 불필요)
- 종목 코드로 직접 조회 → 관련성 필터링(`is_relevant_news`) 자체가 불필요
- 검색 결과 UI 텍스트 오염(`새 창 열림`, `언론사 선정`, `네이버뉴스` 등) 없음
- 페이지네이션으로 해당일 전량 수집 가능 → 50건 상한 해소

기존 검색 크롤러 아티팩트를 고치는 것은 폐기 예정 소스를 수리하는 것이므로 하지 않는다.

### B-1. 스키마 변경

`Database/migrations/001_news_schema.sql` 신규 생성:

```sql
ALTER TABLE daily_news ADD COLUMN IF NOT EXISTS published_at TIMESTAMP;
ALTER TABLE daily_news ADD COLUMN IF NOT EXISTS target_date DATE;
ALTER TABLE daily_news ADD COLUMN IF NOT EXISTS source VARCHAR(32);
ALTER TABLE daily_news ADD COLUMN IF NOT EXISTS press VARCHAR(64);
ALTER TABLE daily_news ADD COLUMN IF NOT EXISTS article_url TEXT;

CREATE INDEX IF NOT EXISTS idx_news_published_at ON daily_news (published_at);
CREATE INDEX IF NOT EXISTS idx_news_target ON daily_news (ticker, target_date);
CREATE UNIQUE INDEX IF NOT EXISTS uq_news_url ON daily_news (article_url) WHERE article_url IS NOT NULL;
```

- `source`: `'finance_crawl'`, `'search_api'`, `'search_crawl'` 등 수집 경로 식별자. **앞으로 모든 적재에 필수 기입**
- `article_url`: 기사 고유 URL. UNIQUE 인덱스로 중복 재적재를 DB 레벨에서 차단
- 기존 행은 `source = 'legacy'`로 일괄 UPDATE

### B-2. 신규 크롤러 작성

`뉴스데이터/NaverFinanceNews.py` 신규 생성.

요구사항:
- 종목 코드와 날짜 범위를 인자로 받아 해당 기간 기사 목록 수집
- 페이지네이션 끝까지 순회 (마지막 페이지 판별 로직 필요 — 동일 페이지 반복 반환 케이스 방어)
- 추출 필드: 제목, 언론사, 발행 일시(날짜+시각), 기사 URL
- `published_at`은 KST 기준 timezone-naive `datetime`으로 통일
- 요청 간 랜덤 딜레이 유지 (기존 0.7~2초 패턴 준수)
- **연관기사 묶음(중복 기사 접힘) 처리**: 펼침 항목까지 수집할지 여부를 상수로 제어 가능하게 하고 기본값은 수집 안 함
- 텍스트 정제 함수를 별도 분리: 제목 끝 UI 문자열 제거, 연속 공백 정규화, 언론사명 접미사 제거
- `UPSERT`는 `article_url` 기준 `ON CONFLICT DO NOTHING`

**시각 파서는 순수 함수로 분리**하고 `if __name__ == "__main__"` 아래에 검증 케이스를 포함한다:
- `오전 12:30` → `00:30`
- `오후 12:30` → `12:30`
- `오후 1:05` → `13:05`
- 날짜만 있고 시각 없는 경우 → `None` 반환

### B-3. 소규모 검증

전면 수집 전에 **1종목 × 5거래일**만 수집해 다음을 확인하고 정지:
- 제목에 UI 텍스트가 섞이지 않는가
- `published_at`이 전 건 채워지는가
- 하루치 기사 수가 50건 상한에 걸리지 않고 실제 분포를 보이는가
- 동일 스크립트 2회 실행 시 중복 행이 늘지 않는가 (UNIQUE 인덱스 동작 확인)

### 완료 조건

5거래일 샘플의 실제 적재 결과(제목 10건 원문 포함)를 출력하고 **정지**.

---

## Task C — 전면 재수집 (표본 확장)

Task B 검증 통과 후 실행. **이 단계의 목적은 244 → 2,000+ 조합 확보다.**

### C-1. 수집 설계

**변동성 필터를 제거한다.** 전 거래일을 균일하게 수집한다.

이유: 변동성 임계값 필터가 (a) 표본 수를 1/3로 줄이고 (b) 시기별 변동성 차이 때문에 split 간 분포 이동을 만든다. Task A-1이 이 가설을 확정하면 근거가 확보된다.

- 기간: `daily_stock_prices`에 존재하는 전 거래일 (2023-09-01 이후, 필요시 확대)
- 종목: 아래 C-2 참조
- 거래일 목록은 `daily_stock_prices`의 distinct date에서 가져온다 (별도 휴장일 캘린더 만들지 않음)

### C-2. 종목 확장 — 섹터 분산

현재 코드에는 2종목(삼성전자, SK하이닉스)이 등록되어 있으나,
**SK하이닉스는 확장성 검증용 자리표시자이며 현재 분석 대상이 아니다.**
실질 분석 대상은 삼성전자 1종목뿐이다. **다른 섹터를 포함해 5~8종목으로 확장한다.**

사유: 선행연구(정지선·김동성·김종우, 2015)에서 IT 섹터의 뉴스 기반 예측 정확도가 전 섹터 중 최저였다.
IT만으로 구성하면 성능 저하 시 "종목 선정 문제"와 "방법론 문제"가 구분되지 않는다.
섹터를 섞으면 섹터별 성능 비교가 가능해지고, 이는 결과 해석의 독립적인 분석 축이 된다.

후보 (KOSPI 대형주, 뉴스 볼륨 충분):
- 반도체/IT: 005930 삼성전자 (000660 SK하이닉스는 코드에 유지하되 **비활성**, 수집 대상에서 제외)
- 자동차: 005380 현대차
- 화학/2차전지: 051910 LG화학
- 금융: 105560 KB금융
- 바이오: 207940 삼성바이오로직스
- 인터넷: 035420 NAVER

**최종 종목 리스트는 사람이 확정한다. 위 후보를 기준으로 제안만 하고 정지할 것.**

주가 데이터도 함께 확장 필요 → `주가데이터/FinanceData_load.py`의 `TICKERS` 리스트에 추가 후 실행.

### C-3. 소요 시간 산정 후 보고

전면 수집 전에 다음을 계산해 보고한다:
- 총 (종목 × 거래일) 요청 수
- 종목당 하루 평균 페이지 수 (B-3 샘플 기준 추정)
- 랜덤 딜레이 적용 시 예상 총 소요 시간

수 시간이 걸릴 것으로 예상되므로, **중단 후 재개 가능한 구조**로 만든다:
- 진행 상황을 별도 테이블 또는 파일에 체크포인트로 기록
- 이미 수집한 (ticker, date)는 건너뛰기

### 완료 조건

수집 완료 후 다음 지표 출력:
- 고유 (ticker, date) 조합 수 (**목표: 2,000 이상**)
- **종목별 조합 수 — 0건인 종목이 하나라도 있으면 즉시 정지하고 보고**
  (Task A-2에서 확인된 다종목 순회 누락이 재발했는지 판별하는 지점)
- 조합당 기사 수 분포
- `published_at` NULL 비율 (**목표: 5% 미만**)
- 월별 기사 수 추이 — 특정 시기 공백이 없는지

---

## Task D — 윈도우 재정렬 (기존 Task 2와 동일)

Task C 완료 후 착수. 정의:

```
Y = D일 등락률 (또는 초과수익률)
X = (D-1일 15:30] ~ (D일 09:00) 사이 발행 기사
```

- `target_date` = 발행 시각 이후 처음 열리는 거래일
- 거래일 목록은 `daily_stock_prices`의 distinct date 기준
- 주말·공휴일 발행 기사는 자동으로 다음 거래일에 매핑

### 판단 정지 지점

장전 윈도우 적용 후 남는 (ticker, target_date) 조합 수를 출력하고 **정지**.
아래 두 안 중 선택은 사람이 한다.

- **안 A (엄격)**: D-1 15:30 ~ D 09:00
- **안 B (완화)**: D-1 09:00 ~ D 09:00 (24시간). D-1 장중 기사 포함, 누수는 여전히 없음

---

## Task E — 초과수익률 라벨링 (기존 Task 3과 동일)

1. `FinanceDataReader`로 `KS11`(KOSPI) 수집 → `market_indicators` 테이블 적재
2. `excess_return = 종목 change_rate − KOSPI change_rate`, 기준일은 `target_date`
3. 새 컬럼 `excess_label SMALLINT` 추가, 기존 `sentiment_score`는 보존
4. 임계값은 **분포 출력 후 사람이 결정**. 고정 임계값 안과 분위수 기반 안의 클래스 분포를 둘 다 계산해 비교 제시
5. 5-class와 3-class 분포를 모두 출력

**추가 검증**: 라벨 산출 후 train/val/test 각 split의 라벨 분포를 출력하고,
Task 0에서 나타난 분포 이동이 해소되었는지 확인한다. 이것이 Task C 균일 수집의 성패 지표다.

---

## Task F — 조합 비교 실험 (기존 Task 4와 동일)

| 조합 | 뉴스 윈도우 | 라벨 |
|---|---|---|
| 1 (기존) | 전체 D일 기사 | change_rate |
| 2 | 장 전 윈도우 | change_rate |
| 3 | 전체 D일 기사 | 초과수익률 |
| 4 | 장 전 윈도우 | 초과수익률 |

- 각 조합에 대해 **TF-IDF 베이스라인을 먼저** 실행
- KoBERT는 베이스라인이 다수결을 유의하게 상회하는 조합에만 투입
- 다수결 baseline은 반드시 **해당 split의 실제 최빈 클래스** 기준으로 계산할 것
  (Task 0에서 CLAUDE.md의 "20.9%"가 오기였음이 확인됨. 실제 test 최빈은 score=-2, 31.8%)

---

## CLAUDE.md 수정 필요 사항

Task A 착수 전에 아래를 반영한다.

- "다수결 baseline 약 20.9%" → "test 최빈 클래스는 score=-2, 31.8% (macro F1 0.0965)"로 정정
- Task 0 진단 결과(고유 조합 244개, SK하이닉스 0건, 중복 10.8%) 기록
- "계획된 다음 단계" 섹션을 본 개정판 순서(A~F)로 교체

---

## 각 Task 정지 지점 요약

| Task | 정지 시 보고 항목 | 사람이 결정할 것 |
|---|---|---|
| A | 소스 혼재 여부, SK하이닉스 원인, 중복 실태 | 가설 확정 여부 |
| B | 5거래일 샘플 적재 결과 | 크롤러 품질 승인 |
| C | 종목 후보 제안 + 소요 시간 산정 | 최종 종목 리스트 |
| C 완료 | 고유 조합 수, NULL 비율 | 표본 충분 여부 |
| D | 장전 윈도우 적용 후 조합 수 | 안 A / 안 B |
| E | excess_return 분포, 클래스 분포 비교 | 임계값, 클래스 수 |

정지 지점에서 임의 판단으로 다음 단계에 진입하지 않는다.
