# Task T-1: Transformer 어블레이션 베이스라인 (가격+거시지표)

## 배경

Task G(G-0~G-4) 완료로 `feature_loader.py`가 누수 없는 피처 행렬을 반환할 수 있는 상태.
이제 뉴스/감성 피처 없이 **가격+거시지표만으로 학습하는 베이스라인**을 구축한다.
이 성능이 이후 "감성 피처를 추가해서 개선되었는가"를 증명할 기준선이 된다.

> 이 태스크는 순수 모델링이 목적이 아니라, **레이블 정의 → 피처 계산 → 학습 → 평가**로
> 이어지는 파이프라인 자체가 재현 가능하고 자동화 가능한 형태로 구축되는 것이 핵심 목표다
> (프로젝트의 핵심 주제인 MLOps 자동화와 직결). 모델 성능 자체보다 파이프라인 구조를 우선한다.

---

## 최종 결론 (T-1, 2026-08-17 확정 — a안: 정직한 마무리)

### 성능 요약 — RMSE/MAE 중심, 방향성 정확도는 부가 지표

| | RMSE | MAE | 방향성 정확도(부가) |
|---|---|---|---|
| Transformer(v1 구피처, **test**, 체크포인트4) | 0.0328 | 0.0267 | 39.53% (baseline 58.14%) |
| 선형회귀(v2, val) | 0.0211 | 0.0156 | 49.77% (baseline 50.70%) |
| Transformer 소형(v2, val, 7시드 평균) | 0.0209 | 0.0153 | **47.51% ± 4.86%p** (baseline 50.70%) |
| Transformer 기존(v2, val) | 0.0209 | 0.0155 | 45.12% (baseline 50.70%) |

**핵심 결론: 가격+거시지표(현재 피처 구성) 단독으로는 익일 방향성 예측이 다수클래스
baseline·무작위 예측과 통계적으로 구분되지 않는다.** RMSE/MAE는 피처 재설계(레벨→비율
변환)로 v1 대비 유의미하게 개선됐지만(0.0328→0.0209대), 이는 "가격이 대체로 크게 안
움직인다"를 잘 맞히는 회귀적 개선이지 방향(상승/하락)을 맞히는 것과는 별개 문제다. 아래
진단 과정 4단계가 전부 같은 결론으로 수렴했다.

### 오늘 진단 과정 (순서대로)

1. **비정상성 문제 발견·수정** (`diagnose_baseline.py`, `dataset_builder.py::build_base_dataset_v2`):
   OHLCV를 레벨로 쓰면 train(45,800~91,000원)과 test(53,000~162,400원) 가격 레벨 자체가
   달라 표준화 시 test가 학습 범위 밖으로 외삽됨. 레벨→수익률/비율 변환(v2)으로 RMSE 0.0310→
   0.0209, val/train loss 비 1.597→0.760로 과적합도 개선.
2. **다중공선성 진단** (`diagnose_baseline.py`, 히트맵 `가격예측/checkpoints/feature_correlation_heatmap.png`):
   |r|>0.8 피처 쌍 26건(OHLC끼리 r≈0.998, 국고채 3·10년 r=0.989 등). 개별 피처-target
   상관계수는 전부 |r|<0.09로 사실상 무상관 — 애초에 선형 신호가 거의 없다는 것을 이 시점에
   이미 확인.
3. **시드 재현성 테스트** (`seed_stability_check.py`, 7시드): 소형 Transformer가 seed=42에서
   baseline을 +1.40%p 넘긴 결과가 나왔었으나, 7시드 평균은 47.51%±4.86%p로 **baseline보다
   오히려 낮음** — 이전 "성공"이 유리한 초기화의 우연이었음을 확인.
4. **PR curve vs 무작위 sanity check** (`pr_curve_vs_random.py`, 5시드 랜덤 band 비교,
   `가격예측/checkpoints/pr_curve_vs_random.png`): A/B/C 전부 recall 0.10~0.90 구간에서
   무작위 band 상단을 넘는 비율이 0~1.2%. **신호 부재를 최종 확정.** 버그가 아니라 근본적
   신호 부재라는 결론.

### 학계 벤치마크와의 일치

가격(가격+거래량) 데이터만으로 익일 방향을 예측하는 과제는 선행연구에서도 baseline을
크게 못 넘는 것으로 보고된다:

- Fischer & Krauss(2018)는 S&P 500 편입종목 전체(1992~2015)에 LSTM을 적용해 가격 데이터만으로
  일별 방향성 정확도 약 56%를 보고했다 — 무작위(50%)보다는 높지만 대다수 방법론에서 재현
  가능한 상한이 50%대 중반에 머문다는 것을 보여주는 대표 사례. 본 프로젝트의 val 결과
  (baseline 대비 우위 없음, 무작위 band 안에 묻힘)는 이보다도 낮은 수준이며, 방향은 같다
  (가격 단독 정보의 방향 예측력이 매우 제한적).
- 효율적 시장 가설(Fama, 1970)의 이론적 예측과도 일치한다: 공개된 가격·거래 정보만으로는
  초과 예측력을 얻기 어렵다는 것이 이론적 기대이며, 오늘 결과는 이를 실증적으로 재확인한
  사례로 볼 수 있다.
- ⚠️ 70~90%대의 훨씬 높은 방향성 정확도를 보고하는 논문들도 존재하나, 대부분 다른 과제
  정의(초단기 인트라데이 구간, 다른 피처군)를 쓰거나 데이터 누수 가능성이 지적되는 경우가
  많아 본 프로젝트와 방법론이 가장 근접한 Fischer & Krauss(2018)를 1차 벤치마크로 채택했다.
  (참고문헌 절 참조)

### 트랙 1(뉴스/감성) 재개 근거로의 연결

이 결과는 "가격 정보만으로는 부족하다"는 것을 실증한 것이지, "이 문제 자체가 풀 수 없다"는
뜻이 아니다. 프로젝트의 원래 설계(주가+뉴스 결합)가 상정한 구조 그대로다: **가격+거시지표
단독 baseline이 무작위 수준이라는 것이 이제 정량적으로 확정됐으므로, 다음 단계로 뉴스
감성 피처를 추가했을 때 이 baseline 대비 개선이 나타나는지를 검증하는 것이 논리적으로
타당한 다음 실험이 된다.** 이는 CLAUDE.md 로드맵의 "Transformer 트랙" 절이 애초에
상정했던 ablation 설계("감성 피처 없이 학습한 결과가 ablation baseline이 된다")와
정확히 일치하며, 오늘 결과로 그 baseline이 구체적 수치(무작위 수준)로 확정된 것이다.
뉴스/감성 트랙(Task B) 재개 여부와 시점은 여전히 사람이 명시적으로 결정할 사안이다.

### 최종 test셋 평가 시점 — 미루기로 확정 (2026-08-17)

test는 체크포인트4(v1 구피처)에서 1회만 사용했다. v2(피처 재설계) 이후 모델의 test 평가는
**트랙 1(뉴스/감성) 결합 후 "가격 단독 vs 가격+뉴스" 최종 비교 시점까지 미루기로 확정**.
이유: test는 한 번만 써야 어블레이션 비교로서 의미가 있는데, 지금 v2를 먼저 보면 그
비교의 첫 test peek을 미리 써버리는 셈이 된다. 트랙 1 재개 시, 그 시점의 최종 가격+거시
모델과 가격+거시+뉴스 모델을 test에서 동시에 한 번만 비교한다.

---

## 설계 결정 사항 (2026-08-17 확정)

### 1. 레이블 (예측 타겟) — 연속값 회귀

```
target_t = (close_t − close_{t-1}) / close_{t-1}   # 익일 절대수익률(%)
```

- **초과수익률(KOSPI 대비 상대성과)이 아니라 절대수익률**을 타겟으로 확정.
  - 사유: 이 프로젝트의 실용적 목적("이 종목을 사도 되는가")은 시장 대비 상대 성과가 아니라
    절대적인 가격 방향을 답해야 함. 초과수익률은 여러 종목 중 상대 비교에는 적합하지만
    단일 종목(삼성전자) 매수 판단에는 부적합하다고 판단.
  - 참고(김범수, 2021 박사논문 비교): 선행연구도 절대 가격 변동 기반 레이블(2~3 클래스 분류)을
    사용했으며, "변동성 미고려"를 스스로 한계로 명시함. 본 프로젝트는 변동성 미고려 문제를
    레이블이 아닌 **피처(아래 3번 참고)**에서 해결하는 방식으로 접근.
- **분류가 아닌 회귀**로 확정.
  - 사유: 분류는 클래스 불균형(보합/상승이 급등락보다 훨씬 잦음) 시 다수 클래스 베이스라인만
    찍어도 높은 accuracy가 나오는 착시 위험이 있음 (KoBERT 1차 실패 시 macro F1이 다수 클래스
    기준선보다 낮았던 사례와 유사한 함정). 회귀는 이 착시가 구조적으로 어려움.
  - 회귀 예측값의 부호(+/-)만 사후 추출하면 방향성 정확도도 부가 지표로 계산 가능
    (두 목적을 동시에 충족).

### 2. 피처 구성 — OHLCV + 거시지표 9종 + lag 적용 초과수익률(모멘텀 신호)

기존 `feature_loader.py`가 반환하는 피처(종목 OHLCV + KOSPI/KOSDAQ/USD_KRW + ECOS 6종)에 더해,
**과거 시점(t-1 이하)의 초과수익률 z-스코어**를 모멘텀 신호로 추가한다.

```
excess_return_z_(t-1) = (종목_등락률_(t-1) − KOSPI_등락률_(t-1)) / rolling_60d_σ(...)
```

- **당일(t) 값은 절대 사용 불가** — 예측 시점에 아직 확정되지 않은 정보(누수)이므로 반드시
  t-1 이하 값만 사용. 레이블(target_t)과 피처(...at t-1)의 인덱스 정렬을 명시적으로 검증할 것
  (off-by-one 오류가 가장 흔한 누수 원인).
- rolling 60일 σ 계산 시에도 t-1 시점까지만 포함 (G-1에서 확정한 "trailing σ는 계산 시점 포함
  금지" 원칙을 t-1 기준으로 동일 적용).
- **자기가중치 편향(self-weighting bias) 관련 메모**: 삼성전자가 KOSPI 시가총액의 상당 비중을
  차지해 KOSPI 자체가 삼성전자 움직임에 영향받는 구조적 문제가 있음. 이 피처가 레이블이 아니라
  입력 피처이므로 심각한 편향은 아니라고 판단해 그대로 사용하기로 확정했으나, 신호 품질이
  기대보다 낮을 수 있다는 점을 알고 있을 것. 삼전 제외 코스피를 별도 계산하는 등의 개선은
  현재 스코프에서 보류.

### 3. 다중공선성 — 지금은 보류, 학습 후 사후 검증

- 여러 피처(초과수익률 파생값, 국고채 3년/10년 등)가 서로 정보가 겹칠 가능성이 있음.
- 이는 데이터 누수와는 무관한 별개 문제(둘 다 과거 시점 정보이므로 누수 아님)이므로,
  **지금 단계에서 임의로 피처를 제거하지 않는다.** 추측성 판단으로 피처를 빼지 말 것.
- 베이스라인 학습 완료 후, 상관관계 히트맵 또는 어블레이션(피처 하나씩 제외하며 성능 비교)으로
  실증적으로 검증한다. 이 검증은 T-1 완료 후 별도 태스크(T-2 이후)로 진행.

### 4. 시퀀스 길이(lookback window) — 20일 고정으로 시작 (2026-08-17 확정)

- 20일 고정. 60일과 데이터 비용은 동일(워밍업 60일에 이미 포함)하나, 학습 표본(train 1,004행)
  대비 과적합 리스크를 최소화하기 위해 짧은 쪽으로 시작. 20 vs 60 비교는 T-1 범위 밖 — 필요 시
  구현 확대 후 별도 실험.

### 5. 학습/검증/테스트 분할 — 70/15/15, 날짜 확정 (2026-08-17 확정)

- 시계열이므로 **랜덤 분할 금지**, 시간순 분할 사용.
- **스트레스(제외) 구간: 2026-02-02 ~ 2026-07-31** (121거래일, `daily_stock_prices` 최종일까지).
  005930 일별 |등락률| 10일 이동평균이 2026-02-02(-6.3%)/02-03(+11.4%)를 기점으로 기존 1.3~2.0%
  대에서 3~6%대로 급변, 이후 데이터셋 끝까지 지속. KOSPI 지수 월평균 |등락률|로도 동일 시점에서
  교차검증됨(2026-01: 1.15% → 02: 2.49% → ... → 07: 4.89%).
  - ⚠️ CLAUDE.md의 잠정 경계(2026-04~2026-07, 뉴스/감성 트랙용)와 다르다. 이 값은 **Task T
    전용으로 확정**한 것이며, 뉴스/감성 트랙(D-3)의 경계일은 별도로 사람이 확정해야 한다
    (아직 미결).
- **탐지 로직과 운영 설정 분리** (2026-08-17 확정):
  - 확정값(`STRESS_PERIOD_START = 2026-02-02`, `STRESS_PERIOD_END = 2026-07-31`)은
    `config.py` 또는 `constants.py`에 **하드코딩 상수**로 저장. 학습 파이프라인은 이 상수를
    그대로 읽어 쓸 뿐, 실행 시마다 재탐지하지 않는다.
  - 상수 옆에 탐지 근거를 주석으로 남긴다: "005930 일별 |등락률| 10일 이동평균 기준 변곡점,
    2026-08-17 확정".
  - 탐지에 쓴 이동평균 변곡점 탐색 로직은 `analysis/detect_stress_period.py`(또는 유사 경로)에
    일회성 분석 도구로 별도 보관. 학습 파이프라인 코드와 분리해 자동 재탐지 경로를 만들지 않는다.
  - 향후 데이터 추가로 재평가가 필요해지면: 자동 갱신 금지. 사람이 이 스크립트를 수동
    재실행 → 새 경계값을 확인 → 상수를 수동으로 갱신 → 갱신 사유를 주석/커밋 메시지에 기록,
    순서로 처리한다.
- **정상 레짐(사전 추정치)**: 2020-01-02 ~ 2026-02-01 (1,494거래일), 60일 σ 워밍업만 고려한
  추정 — 2020-03-30 ~ 2026-01-30 (1,434거래일)로 잡았었음.
- **정상 레짐(체크포인트 3 실측치, 2026-08-17 확정)**: base 피처(체크포인트 1) + momentum
  피처(체크포인트 2)를 실제로 merge한 뒤 dropna한 결과, momentum 쪽 워밍업이 62행(σ 60일 +
  excess_return pct_change 1일 + 최종 shift 1일)이라 사전 추정(60행)보다 2행 더 소모됨.
  실측 정상 레짐 = **1,432행 (2020-04-01 ~ 2026-01-30)**.
  - 70/15/15 (실측 1,432행 기준):
    - train: 1,002행 (2020-04-01 ~ 2024-04-23)
    - val: 215행 (2024-04-24 ~ 2025-03-17)
    - test: 215행 (2025-03-18 ~ 2026-01-30)
  - 스트레스 구간(1,432행과 별도): 121행 (2026-02-02 ~ 2026-07-31)
  - **test 마지막 거래일(2026-01-30) → 스트레스 첫 거래일(2026-02-02) 사이 갭 0 거래일** —
    바로 다음 거래일이 스트레스 시작일. `가격예측/split_dataset.py`가 이 겹침 없음을 코드로
    assert. 정상 레짐(1,432행) 전체가 스트레스 시작일 이전, 스트레스(121행) 전체가
    상수 범위 안에 있음을 검증 통과.
- `feature_loader.get_features('005930','2020-01-02','2026-01-30')` 검증 결과 14개 컬럼(OHLCV 5
  + 지표 9) 전 구간 NaN 0% — lookback 20일 구간에 결측 문제 없음.

### 6. 모델 규모 — 최소 구성으로 시작 (2026-08-17 확정)

- train 1,004행 × 15피처(OHLCV 5 + 지표 9 + excess_return_z 1) 규모에 맞춰 최소 구성:
  encoder 1~2 layer, d_model 32~64, head 2~4, dropout 0.2~0.3.
- 이 구성도 과적합 여지가 있으므로 학습 곡선(train/val loss 괴리)을 반드시 모니터링하고,
  필요시 더 축소.

---

### 체크포인트4 이후 반복 실험 로그 (2026-08-17, test 미사용 — val로만 판단)

체크포인트4 결과(방향성 정확도 39.53%, baseline 58.14%에 -18.60%p)가 저조해 모델을 바로
건드리지 않고 원인 진단 → 피처 재설계 → 모델 규모 축소 순으로 진행 중. **test는 아직 한 번도
다시 보지 않았다** — 아래는 전부 val 기준.

1. **진단** (`가격예측/diagnose_baseline.py`): 개별 피처-target 상관계수 전부 |r|<0.09(무상관에
   가까움), 피처간 다중공선성 심각(|r|>0.8 쌍 26건, OHLC끼리 r≈0.998), train/test 종가 레벨
   자체가 다름(train 45,800~91,000원 vs test 53,000~162,400원) → 레벨 피처 표준화가 test에서
   외삽되는 구조적 문제 확인.
2. **피처 재설계 v2** (`가격예측/dataset_builder.py::build_base_dataset_v2`): OHLC 레벨 ->
   close_return/hl_range_ratio/open_gap_ratio, volume -> log1p, 국고채 3·10년 -> 스프레드로
   축소 (15피처 -> 13피처). 결과(`가격예측/train_v2.py`): val RMSE 0.0310->0.0209,
   val/train loss 비 1.597->0.760(과적합 개선)로 회귀 지표는 좋아졌지만, **방향성 정확도는
   49.77%->45.12%로 오히려 하락**(val baseline 50.70%도 둘 다 못 넘음) — 스케일/외삽 문제는
   고쳤지만 방향 신호 문제는 그대로.
3. **모델 규모 축소 + 선형회귀 재확인** (`가격예측/compare_v2_variants.py`, v2 피처 위 3-way):

   | | 파라미터 | val RMSE | val MAE | val 방향성정확도 | baseline(50.70%) 대비 |
   |---|---|---|---|---|---|
   | A) 선형회귀(v2) | 14 | 0.0211 | 0.0156 | 49.77% | -0.93%p |
   | **B) Transformer 소형(d16/L1)** | 2,785 | 0.0209 | 0.0153 | **52.09%** | **+1.40%p** |
   | C) Transformer 기존(d32/L2) | 18,209 | 0.0209 | 0.0155 | 45.12% | -5.58%p |

   **B(소형 Transformer, d_model=16/layer=1)가 val baseline을 처음으로 넘긴 유일한 조합**
   (+1.40%p). RMSE/MAE는 세 조합이 사실상 동일 — 차이는 방향성 정확도에서만 발생.
   B는 patience 소진 전 최대 epoch(62)에 도달해 조기종료가 걸리지 않음 — 더 학습하면 개선
   여지가 남아있을 수 있음(단, val_loss는 epoch 40 이후 0.00044~0.00046대에서 정체 중이라
   추가 개선 폭은 제한적으로 보임).

   다음 단계(B 설정 확정, 추가 축소, 또는 test 최종 확인 진행 여부)는 사람 확인 후 진행.

4. **하락 클래스 진단 + 게이트 기준 확정** (`가격예측/diagnose_down_class.py`, val, test 미사용):
   threshold=0 기준 confusion matrix에서 A(선형회귀)는 val 90.7%를 "하락"으로 찍어 recall만
   높고(89.91%) precision은 baseline과 동일(50.26%) — F1/F2가 이런 편향을 보상 없이 보상하는
   함정 확인. PR curve는 recall 0.3~0.9 전 구간에서 precision이 44~53%로 거의 평평 —
   임계값 튜닝으로 얻을 이득이 거의 없음. **확정(사용자 승인): precision이 baseline(하락 비율)
   대비 +2%p 이상일 때만 F2 비교 대상으로 인정하는 게이트 도입, recall 최소선 0.5.**

5. **B(소형 Transformer, d16/L1) 시드 재현성 확인** (`가격예측/seed_stability_check.py`,
   7개 시드, val, test 미사용) — **경고: 이전 seed=42의 "baseline 돌파"는 재현되지 않는다.**

   | | mean | std | min | max |
   |---|---|---|---|---|
   | 방향성 정확도 | **47.51%** | ±4.86%p | 39.53%(seed777) | 52.56%(seed7) |
   | Precision(하락) | 49.23% | ±4.15%p | - | - |
   | Recall(하락) | 64.74% | ±21.57%p | - | - |
   | F2(하락) | 0.600 | ±0.173 | - | - |

   게이트(precision≥52.70%) + recall≥0.5 동시 통과: **7개 중 2개(seed 42, 123)뿐.**
   시드 평균 방향성 정확도(47.51%)가 baseline(50.70%)보다 오히려 낮다 — **직전 턴에서
   "B가 baseline을 처음 넘겼다"고 본 것은 seed=42라는 유리한 초기화의 우연이었을 가능성이
   높고, 재현 가능한 실제 신호로 보기 어렵다.** 위 3번 항목의 결론("B가 유일한 성공 조합")은
   이 재현성 결과로 사실상 철회. 근본 원인 진단(개별 피처 상관계수 |r|<0.09)과 다시 일치하는
   방향 — 아직 견고한 방향성 신호를 찾지 못한 상태.

6. **PR curve sanity check: 무작위 예측과 비교** (`가격예측/pr_curve_vs_random.py`, val,
   test 미사용) — **A/B/C(seed=42) 전부 무작위 예측(5시드 평균+min~max band)과 통계적으로
   구분이 안 된다.**

   recall 0.10~0.90 구간에서 모델 곡선이 무작위 band 상단(max)을 넘는 비율: A 0.0%, B 0.0%,
   C 1.2% (사실상 0). 무작위 평균 대비 평균 gap: A −0.0244, B −0.0073(둘 다 무작위 평균보다
   낮음), C +0.0097(사실상 0에 가까운 미미한 우위, band 안에 완전히 묻힘).

   **최종 판정: 버그가 아니라 근본적 신호 부재.** 가격+거시지표(현재 피처 구성)만으로는
   하락 방향에 대한 어떤 임계값에서도 무작위보다 나은 precision-recall 트레이드오프를
   얻지 못한다. 지금까지의 진단(개별 피처 상관계수 |r|<0.09, 시드 간 방향성 정확도
   47.51%±4.86%p로 baseline 이하)과 이 sanity check가 모두 같은 결론을 가리킨다.
   다음 단계 판단(T-1을 회귀 성능 위주로 마무리 vs 추가 구조 변경 vs 뉴스/감성 트랙 결합)은
   사람 확인 후 진행.

---

## 참고문헌

- Fischer, T., & Krauss, C. (2018). Deep learning with long short-term memory networks for
  financial market predictions. *European Journal of Operational Research*, 270(2), 654–669.
  — S&P 500 편입종목(1992~2015)에 LSTM을 적용, 가격 데이터만으로 일별 방향성 정확도 약 56%
  보고. 본 프로젝트와 방법론이 가장 근접한(가격 단독, 일별 방향, 딥러닝) 벤치마크로 채택.
- Fama, E. F. (1970). Efficient Capital Markets: A Review of Theory and Empirical Work.
  *The Journal of Finance*, 25(2), 383–417. — 효율적 시장 가설의 이론적 근거.
- (기존 인용, CLAUDE.md에서 이어짐) Antweiler, W., & Frank, M. Z. (2004); Tetlock, P. C.
  (2007~2011); 정지선·김동성·김종우 (2015), 지능정보연구 21권 4호; 김범수 (2021, 박사논문).

---

## 체크포인트 (에이전트는 여기서 멈추고 보고)

1. 레이블·피처 계산 스크립트 초안 작성 후 — 인덱스 정렬(레이블 t vs 피처 t-1) 검증 결과 보고
2. lag 적용 초과수익률 피처 계산 결과 샘플(누수 검증: 특정 날짜 피처의 원천 데이터가 실제로
   그 전날 이전 것인지 샘플 확인) 보고
3. 학습/검증/테스트 분할 결과(구간별 행 수, 2026 Q2-Q3 제외 구간 확인) 보고
4. 베이스라인 모델 첫 학습 완료 후 — RMSE/MAE + 방향성 정확도(다수 클래스 베이스라인과 비교)
   결과 보고
5. 스트레스 구간 홀드아웃 평가 완료 후 — 정상 test / naive baseline(스트레스 구간) / 학습 모델
   (스트레스 구간) 3자 비교 결과 보고

## 완료 조건 (Definition of Done)

- [x] 레이블·피처 파이프라인이 `feature_loader.py`를 재사용/확장하는 형태로 구현됨
      (`가격예측/dataset_builder.py`, `momentum_feature.py`, v1→v2 확장 포함)
- [x] 누수 검증: 레이블-피처 인덱스 정렬 및 rolling σ 계산 범위가 명시적으로 확인됨
      (체크포인트1/2, 날짜·값·정의식 3중 대조로 검증 통과)
- [x] 시간순 분할 적용, 2026 Q2-Q3 구간 별도 분리 확인
      (체크포인트3, `split_dataset.py`, 스트레스 구간 2026-02-02~2026-07-31 겹침 없음 assert 통과)
- [x] 베이스라인 모델 학습 완료, RMSE/MAE 및 방향성 정확도(다수 클래스 baseline 대비) 보고
      (2026-08-17, `가격예측/train.py`): test RMSE=0.0328, MAE=0.0267, 방향성 정확도 39.53%
      vs 다수 클래스 baseline("항상 상승 예측") 58.14% — **모델이 baseline보다 18.60%p 낮음**
- [x] 다중공선성은 임의 판단으로 처리하지 않고 사후 검증 태스크로 실제 진행함
      (`diagnose_baseline.py`, |r|>0.8 쌍 26건 확인 → v2에서 OHLC 통합·국고채 스프레드화로
      일부 해소. 나머지 그룹(KOSPI 등)은 이번 스코프에서 미해소로 남겨둠)
- [x] 스트레스 구간(2026-02-02~2026-07-31) 상수가 `constants.py`에 하드코딩되고,
      탐지 스크립트(`analysis/detect_stress_period.py`)는 파이프라인과 분리되어 일회성 도구로만
      존재함 (자동 재탐지 없음) — `가격예측/split_dataset.py`가 이 상수를 import해서만 사용
- [ ] 스트레스 구간 홀드아웃 평가 완료: 학습된 베이스라인 모델을 스트레스 구간에 그대로 추론해
      (a) 정상 test셋, (b) naive baseline(전일값 유지)의 스트레스 구간 성능, (c) 학습 모델의
      스트레스 구간 성능 — RMSE/방향성 정확도 3자 비교표로 보고
      **(미착수 — a안 확정으로 정상 레짐 방향성 신호 부재가 먼저 확정돼 우선순위 밀림.
      정상 레짐에서도 무작위 수준인 모델을 스트레스 구간에 돌리는 게 지금 의미가 크지
      않다고 판단해 보류. 재개 여부는 사람 결정 필요)**
- [ ] 스트레스 구간에서의 성능 저하는 실패가 아니라 **"모델의 정상 레짐 의존성" 한계**로
      프레이밍해 기록 (CLAUDE.md의 레버리지 ETF 강제 청산 분석과 일관된 결론 방향)
      **(위 항목 미착수로 함께 보류)**
