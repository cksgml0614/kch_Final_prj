# 작업 지시서: market_indicators 적재 (Task G)

> 이 작업은 뉴스/감성 트랙과 독립적이다. 크롤링 재수집과 병행 가능하다.
> 산출물은 두 트랙 모두가 사용한다:
> - **트랜스포머 입력 피처** (주가 + 거시지표만 쓰는 ablation baseline)
> - **초과수익률 라벨링 기준값** (KOSPI 등락률)

---

## 공통 규칙

- 프로젝트 루트에서 `python -m` 형태로 실행 가능해야 한다
- API 키는 `.env`에서만 읽는다 (`ECOS_API_KEY` 추가 필요)
- 기존 테이블을 DROP하지 않는다. 스키마 변경은 `ALTER TABLE` + `Database/migrations/` 파일
- 각 단계 완료 시 요약 출력하고 다음 단계로 임의 진행하지 않는다

---

## G-0. 현재 스키마 확인 (선행) — 완료

`market_indicators`는 테이블만 정의되어 있고 적재 이력이 없다. 먼저 실제 DDL을 확인한다.

- `Database/create_tables.sql`의 `market_indicators` 정의 출력
- 실제 DB의 컬럼 구성 확인 (`\d market_indicators` 또는 `information_schema.columns`)
- 현재 행 수 확인 (0일 것으로 예상)

**확인 후 정지.** 아래 G-1의 스키마 설계가 현재 정의와 맞는지 사람이 판단한다.

---

## G-1. 스키마 설계 — 완료

### 채택 방식: long format

지표별로 컬럼을 만들지 않고, `(indicator_code, date, value)` 형태의 long format을 유지한다.
지표 추가 시 스키마 변경이 필요 없고, 주기(일/월/분기)가 다른 지표를 한 테이블에 담을 수 있다.

- `date`: 지표의 **기준일**(reference date). CPI 2026년 6월분이면 2026-06-01
- `published_date`: 그 값이 **실제로 공표된 날**. CPI 6월분은 7월 초에 발표됨
- `value`: 지표값

### `published_date`가 필요한 이유 — 미래 정보 누수 방지

거시지표는 기준일과 공표일이 다르다. 2026년 6월 CPI를 6월 시점 피처로 쓰면, 당시에는 존재하지 않던 정보를 쓰는 것이다. **트랜스포머 학습 시 반드시 `published_date <= 예측일` 조건으로 조인해야 한다.**

일별 지표(환율, 국고채 금리)는 `published_date = date`로 두면 된다.
월별 지표는 실제 공표일을 넣되, 확인이 어려우면 **보수적으로 기준월 다음달 말일**로 설정하고 그 사실을 기록한다.

### 지표 메타 테이블 (확정, `002_market_indicators.sql` 반영)

```sql
CREATE TABLE IF NOT EXISTS indicator_meta (
    indicator_code VARCHAR(32) PRIMARY KEY,
    name           VARCHAR(128) NOT NULL,
    source         VARCHAR(32)  NOT NULL,   -- 'FDR' | 'ECOS'
    frequency      VARCHAR(8)   NOT NULL,   -- 'D' | 'W' | 'M' | 'Q'
    unit           VARCHAR(32),
    ecos_stat_code VARCHAR(32),             -- ECOS 통계표코드
    ecos_item_code VARCHAR(32),             -- ECOS 항목코드
    note           TEXT
);
```

지표가 늘어나면 코드만 보고는 무엇인지 알 수 없다. 메타 테이블 없이 진행하지 않는다.

### 사용 계약 (6가지, `002_market_indicators.sql`에서 최종 확정)

1. **메타 선삽입**: 지표 적재 전 `indicator_meta`에 행이 먼저 존재해야 함 (FK 위반 방지)
2. **삭제 순서**: 재적재 시 `market_indicators` 먼저 삭제 → `indicator_meta` 나중 (FK `ON DELETE RESTRICT`)
3. **published_date 명시 대입**: DEFAULT 금지. 로더 코드에서 반드시 명시적으로 대입
4. **indicator_code 명명 규칙**: `ECOS_<통계표코드>_<항목코드>` 또는 FDR 심볼 그대로
   — *단, G-2에서 alias 매핑 방식으로 예외 적용 (아래 G-2 참고)*
5. **upsert 방식**: `INSERT ... ON CONFLICT (indicator_code, date) DO UPDATE SET value = EXCLUDED.value, published_date = EXCLUDED.published_date WHERE market_indicators.value IS DISTINCT FROM EXCLUDED.value` — `RETURNING`으로 개정 건수 카운트
6. **파생값 DB 저장 금지**: 등락률 등은 저장하지 않고 조회 시 `LAG(value)`로 계산

**설계안 출력 후 정지.** 사람 승인 후 마이그레이션 실행. → 완료, 커밋 예정.

---

## G-2. 지수·환율 데이터 적재 (FinanceDataReader) — 완료

`시장지표/market_index_load.py` 신규 생성. (폴더명은 기존 한글 폴더 관례를 따름)

> ⚠️ **원본 설계 대비 변경 사항 (2026-08-16 확정)**
> - 지표 범위: KS11/KQ11 2개 → **KOSPI/KOSDAQ/USD_KRW 3개로 확장** (환율 추가)
> - 명명 방식: 심볼 그대로 → **alias 매핑 방식**으로 변경 (아래 참고)
> - upsert 방식: `ON CONFLICT DO NOTHING` → **G-1에서 확정된 `IS DISTINCT FROM` 기반 upsert로 통일** (개정치 반영 필요)

### 수집 대상

| indicator_code (alias) | FDR 심볼 (source_symbol) | 내용 |
|---|---|---|
| `KOSPI`   | `KS11`    | 코스피 지수 |
| `KOSDAQ`  | `KQ11`    | 코스닥 지수 (전체 시장 흐름 보완용) |
| `USD_KRW` | `USD/KRW` | 원/달러 환율 |

- **기간**: FDR 제공 최대 히스토리 전체 (기간 제한 없이 수집)
  - 데이터 수집과 모델링(윈도우 컷)을 분리하는 원칙. 학습 시점에 필요한 구간만 잘라 쓴다.
  - 과거 데이터가 실제로 모델 성능에 유의미한지는 G-2 스코프 밖. Transformer 어블레이션
    베이스라인 설계 시(G-4 이후) 재검토 대상으로 플래그.
- 증분 수집 로직은 `주가데이터/FinanceData_load.py` 패턴을 따른다 (`MAX(date)` 조회 → 이후만 수집)
- **upsert**: G-1에서 확정된 `IS DISTINCT FROM` + `RETURNING` 패턴 사용 (`ON CONFLICT DO NOTHING` 아님, 위 계약 5번 참고)
- `published_date = date`

### indicator_code 명명 — alias 매핑 방식 (임시)

스키마(`indicator_meta`) 변경 없이, 로더 스크립트 내부 딕셔너리로 alias ↔ 실제 심볼을 매핑한다.

```python
# 임시 alias 매핑. 추후 테이블 구조 통합 리팩터링 시 재검토 대상.
# (옵션: indicator_meta.source_symbol 컬럼 신설로 흡수 가능 — 지금은 보류)
INDICATOR_MAP = {
    "KOSPI":   "KS11",
    "KOSDAQ":  "KQ11",
    "USD_KRW": "USD/KRW",
}
```

- `indicator_meta.indicator_code`에는 alias(`KOSPI` 등)를 저장한다.
- FDR 호출 시에는 매핑을 거쳐 실제 심볼(`KS11`, `USD/KRW`)을 사용한다.
- `indicator_meta.note`에 alias 매핑 방식임을 명시 (예: `"FDR 심볼: KS11 (alias 매핑, source_symbol 컬럼 없음)"`)
- **주의**: 이 방식은 임시다. 나중에 테이블 통합 시 alias 매핑 로직 자체가 사라지거나 스키마
  컬럼으로 흡수될 수 있음.

### 등락률은 저장하지 않는다

`change_rate`를 별도 지표로 넣지 않고, 조회 시 `LAG(value)`로 계산한다.
사유: 파생값을 저장하면 원본과 불일치할 여지가 생긴다. 초과수익률 계산 시 뷰나 CTE로 처리한다.

단, 검증용으로 **KOSPI 일별 등락률의 기술통계를 출력**할 것 (평균, 표준편차, 최대/최소, 분기별 평균 절대값).
2026년 6~7월 구간에서 일간 ±6~11% 수준이 나오는지 확인 — 나오면 데이터 정상이다.

### 열린 질문 (구현 중 확인 필요)

- `unit` 값을 KOSPI/KOSDAQ에 `'point'`로 넣을지 `NULL`로 둘지
- `USD_KRW`의 FDR 반환 컬럼이 종가(Close) 기준인지 확인 필요 (시가/고가/저가/종가 중 어떤 컬럼을 `value`로 매핑할지)

### 체크포인트 (에이전트는 여기서 멈추고 보고)

1. `INDICATOR_MAP` 및 `indicator_meta` 삽입 값(특히 `unit`, `note`) 초안 작성 후 — 보고 및 확인
2. FDR로 3개 지표 수집 결과 샘플(행 수, 기간 범위, 결측치 여부) 확인 후 — 보고 및 확인
3. `market_indicators` 적재 전 dry-run 결과(적재 예정 건수) 확인 후 — 실제 커밋 여부 확인
4. 적재 완료 후 최종 요약(지표별 건수, 기간, 실패 여부, KOSPI 등락률 기술통계) 보고

### 완료 조건

- [x] `indicator_meta`에 KOSPI, KOSDAQ, USD_KRW 3행 존재
- [x] `market_indicators`에 3개 지표 전체 히스토리 적재
- [x] `published_date`가 모두 NOT NULL이며 `date`와 동일 (일별 지표 원칙)
- [x] 재실행 시 upsert가 정상 동작(중복 삽입 없이 값 변경분만 UPDATE) 확인
- [x] 로그에 지표별 적재 건수 요약 출력 확인
- [x] KOSPI 일별 등락률 기술통계 출력, 2026 Q2-Q3 급락 구간 반영 확인

### 적재 결과 (2026-08-16)

| indicator_code | 건수 | 기간 |
|---|---|---|
| KOSPI | 4,091 | 2010-01-04 ~ 2026-08-14 |
| KOSDAQ | 4,091 | 2010-01-04 ~ 2026-08-14 |
| USD_KRW | 5,890 | 2003-12-01 ~ 2026-08-16 |

합계 14,072행, 실패 0건. 재실행 시 3개 전부 스킵(신규/개정 0건) — upsert 멱등성 확인.
KOSPI 등락률: 2026 Q2 평균 |등락률| 2.83%, Q3 4.20%로 정상 레짐(2024~2025, 0.8~1.4%) 대비 급등. 최대 +17.91%(2026-07-31), 최소 -12.06%(2026-03-04) — CLAUDE.md에 기록된 2026년 상반기 이상 변동성과 방향 일치.

구현 중 발견한 이슈 2건: (1) `USD/KRW`는 `index.name`이 `None`이라 `reset_index()` 컬럼명이 KS11/KQ11과 달라 `KeyError` 발생 → `Date`로 명시 통일. (2) `USD/KRW`가 float32 소스라 이진 부동소수 잡음이 섞여 있어 `round(value, 4)` 적용.

---

## G-3. ECOS 거시지표 적재 — 완료

### G-3-1. 지표 코드 조회 (하드코딩 금지) — 완료

ECOS 통계표코드·항목코드를 **추측하거나 기억으로 채우지 말 것.** 다음 API로 조회해 확정한다.

1. `StatisticTableList` — 통계표 목록에서 대상 통계표 검색
2. `StatisticItemList` — 해당 통계표의 세부 항목코드 확인
3. `StatisticSearch` — 실제 값 조회 테스트 (소량)

조회 결과(통계표코드, 항목코드, 단위, 주기, 데이터 보유 기간)를 표로 출력하고 **정지**.
사람이 코드가 맞는지 확인한 뒤 적재를 진행한다.

> ~~블로커: `.env`에 `ECOS_API_KEY` 미등록~~ → 2026-08-16 등록 완료, 해소됨.

전체 834개 통계표를 조회해 후보별로 `StatisticItemList`·`StatisticSearch`로 실측 검증한 결과:

| 지표 | STAT_CODE | ITEM_CODE | 주기 | 단위 | 보유기간 |
|---|---|---|---|---|---|
| 한국은행 기준금리 | `722Y001` | `0101000` | D | 연% | 1999-05-06 ~ |
| 국고채(3년) | `817Y002` | `010200000` | D | 연% | 1998-11-13 ~ |
| 국고채(10년) | `817Y002` | `010210000` | D | 연% | 2000-12-18 ~ |
| 소비자물가지수(총지수) | `901Y009` | `0` | M | 2020=100 | 1965-01 ~ |
| M2(평잔, 계절조정) | `161Y005` | `BBHS00` | M | 십억원 | 2003-10 ~ |
| 선행지수 순환변동치 | `901Y067` | `I16E` | M | 2020=100 | 1970-01 ~ |
| ~~반도체 수출금액지수~~ | `403Y001` | `3091AA` | M | 2020=100 | 2000-01 ~ | (제외, 사유는 G-3-2 참고) |

국고채 3년/10년은 통계표명에 "국고채"가 없는 "시장금리(일별)"(`817Y002`) 안의 항목이라 `국고채`로 통계표를 검색하면 찾을 수 없었다 — `금리`로 검색해 발견. ECOS에는 종목 단위 원화·달러 금액 기준 반도체 수출 통계는 없고, 무역지수 성격의 "수출금액지수"(2020=100)만 있었다.

### G-3-2. 수집 대상 지표 — 완료 (최종 확정 2026-08-16)

| 확정 지표 | indicator_code | 주기 | 선정 이유 |
|---|---|---|---|
| 한국은행 기준금리 | `ECOS_722Y001_0101000` | D | 통화정책 기조 |
| 국고채 3년 | `ECOS_817Y002_010200000` | D | 단기 금리 |
| 국고채 10년 | `ECOS_817Y002_010210000` | D | 장기 금리. 3년과의 스프레드가 경기 신호 |
| 소비자물가지수(CPI) | `ECOS_901Y009_0` | M | 인플레이션 |
| M2 통화량 | `ECOS_161Y005_BBHS00` | M | 유동성 |
| 경기선행지수 순환변동치 | `ECOS_901Y067_I16E` | M | 경기 국면 |

- 일별 지표는 그대로 저장
- 월별 지표는 기준일을 해당 월 1일로 통일하고, `published_date`를 별도 기입 (공표일 확인이 어려워 기준월 다음달 말일로 보수적 설정, `indicator_meta.note`에 기록)
- 원/달러 환율은 G-2에서 FDR로 이미 적재했고, ECOS 후보 목록에 넣지 않아 중복 없음

**반도체 수출금액지수(`403Y001`/`3091AA`) 제외 확정**: 종목 독립적 공통 지표 테이블(`market_indicators`) 설계 원칙과 맞지 않는 섹터 특화 지표로 판단, 2026-08-16 확정 제외. 필요 시 별도 구조(예: 종목-지표 매핑 테이블)로 추후 분리 검토. (현재는 삼성전자 기준으로 모델을 만들지만, 종목을 자유롭게 확장 가능한 공통 구조를 목표로 하기 때문에 섹터 특화 지표를 공통 테이블에 넣지 않기로 함.)

### G-3-3. 구현 — 완료

`시장지표/ecos_load.py` 신규 생성.

- 지표 목록은 코드 상단 상수 또는 `indicator_meta` 테이블에서 읽어 순회
- **종목 순회 버그를 답습하지 않는다**: 지표 하나 실패해도 나머지 계속, 예외는 로그로 남기고 삼키지 않음, 마지막에 **지표별 성공/실패/적재 건수 요약 출력**, 0건 지표는 경고
- API 호출 실패·빈 응답을 구분해서 처리 (키 오류 / 코드 오류 / 해당 기간 데이터 없음)
- 증분 수집: 지표별 `MAX(date)` 조회 후 이후만 요청
- ECOS API는 한 번에 조회 가능한 건수 제한이 있으므로 페이지 분할 처리 (`PAGE_SIZE=10000`, 실측 최대 건수인 기준금리 9,961건도 단일 요청에 수렴해 이번 6개 지표는 페이지 분할이 실제로 발동하지는 않았음)

**리팩터링**: `market_indicators`/`indicator_meta` upsert 로직이 G-2(`market_index_load.py`)와 완전히 동일해서 `시장지표/db_utils.py`로 공통 분리하고, `market_index_load.py`도 이를 쓰도록 리팩터링했다 (동작 변경 없음, 재실행으로 확인).

### 완료 조건 — 결과 (2026-08-16)

| indicator_code | 건수 | 기간 |
|---|---|---|
| ECOS_722Y001_0101000 (기준금리) | 9,961 | 1999-05-06 ~ 2026-08-13 |
| ECOS_817Y002_010200000 (국고채3년) | 6,878 | 1998-11-13 ~ 2026-08-14 |
| ECOS_817Y002_010210000 (국고채10년) | 6,354 | 2000-12-18 ~ 2026-08-14 |
| ECOS_901Y009_0 (CPI) | 739 | 1965-01 ~ 2026-07 |
| ECOS_161Y005_BBHS00 (M2) | 273 | 2003-10 ~ 2026-06 |
| ECOS_901Y067_I16E (선행지수순환변동치) | 678 | 1970-01 ~ 2026-06 |

합계 24,883행, 실패 0건, 결측 스킵 0건. 재실행 시 6개 전부 스킵(신규/개정 0건) — upsert 멱등성 확인.
`published_date` 전량 NOT NULL, 일별 지표 `date == published_date` 불일치 0건, 월별 지표는 "다음달 말일" 원칙대로 반영 확인(예: CPI 2026-07 → published_date 2026-08-31).

---

## G-4. 피처 조회 함수 (트랜스포머 연결 준비) — 완료

`시장지표/feature_loader.py` 신규 생성.

### 요구사항

특정 종목·기간에 대해 **미래 정보 누수 없이** 피처 행렬을 반환하는 함수를 만든다.

```
get_features(ticker, start_date, end_date) -> DataFrame
```

- 인덱스: 거래일 (`daily_stock_prices`의 date 기준)
- 컬럼: 종목 OHLCV + 각 거시지표
- **거시지표 조인 규칙**: 예측 대상일 `D`에 대해, `published_date < D`인 값 중 가장 최근 값을 사용
  (같은 날 공표된 값은 장중 발표 시각이 불명확하므로 보수적으로 제외)
- 월별 지표는 forward-fill (다음 공표까지 직전 값 유지)
- 초기 구간에 값이 없는 지표는 NaN으로 두고, NaN 비율을 함께 반환

### 검증

- 반환된 DataFrame에서 각 컬럼의 NaN 비율 출력
- **누수 검증**: 임의의 월별 지표 하나를 골라, 특정 날짜에 조인된 값의 `published_date`가 실제로 그 날짜보다 이전인지 샘플 10건 확인 출력

### 구현 방식

일별/월별을 분기 처리하지 않고 `merge_asof(direction='backward', allow_exact_matches=False)` 하나로 통일했다.
일별 지표는 `published_date = date`라 자연히 전일 lookback이 되고, 월별 지표는 다음 공표 전까지 값이 유지되는
forward-fill이 같은 로직에서 자동으로 성립한다. `Decimal → float` 캐스팅은 `_load_prices`·`_load_indicator_series`
양쪽 모두 DB 조회 직후 가장 먼저 적용해 G-2에서 발견된 버그(NUMERIC → psycopg Decimal → pandas 연산 충돌)의
재발을 막았다.

### 완료 조건 — 결과 (2026-08-16, 샘플: 005930, 2024-01 ~ 2024-03)

- **shape**: (61, 14) — 61거래일 × (종목 OHLCV 5개 + 지표 9개)
- **columns**: `open, high, low, close, volume` + `KOSPI, KOSDAQ, USD_KRW, ECOS_722Y001_0101000, ECOS_817Y002_010200000, ECOS_817Y002_010210000, ECOS_901Y009_0, ECOS_161Y005_BBHS00, ECOS_901Y067_I16E`
- **NaN 비율**: 전 컬럼 0.00%. 사유: 샘플 종목(005930)의 가격 데이터 시작일(2020-01-01)이 지표 9개 전부의 데이터 시작일보다 늦어, 이 샘플로는 초기 결측 구간이 실제로 재현되지 않음. `merge_asof`가 매치 실패 시 NaN을 반환하는 동작 자체는 구조적으로 보장됨(코드 리뷰로 확인, 실측 재현은 아님).
- **누수 검증** (CPI, `ECOS_901Y009_0`, 10건 샘플): 전체 통과 (`published_date < 거래일` 100%). forward-fill 갱신 시점도 값 변화로 확인됨 — 예: 1/19 거래일까지 112.68(2023-12 CPI, published 2023-12-31) 유지, 2/16 거래일부터 112.73(2024-01 CPI, published 2024-01-31)으로 갱신.

**남은 검증 과제** (완료 조건에는 포함되지 않으나 기록): SK하이닉스 등 다른 종목이나 더 이른 기간으로 실제 NaN 발생 케이스를 재현·검증하는 작업은 아직 하지 않았다. 지금 종목(005930) 샘플로는 초기 결측 구간이 존재하지 않아 재현 불가능했다. 종목이 확장되거나(Task C) 더 이른 시작점을 쓰게 되면 재검증 대상.

---

## G-5. 병행 확인 사항 (크롤링 트랙 선행 조건)

Task G와 무관하지만 동시에 확인해두면 크롤링 착수가 빨라진다.

- **네이버 금융 종목뉴스 페이지의 과거 조회 가능 기간** — 몇 년 전까지 페이지네이션이 유효한지 실제로 확인
  (005930 기준으로 2020년, 2022년, 2023년 시점까지 접근되는지 테스트)
- 이 값이 뉴스 데이터 수집 시작점을 결정한다. 2020년까지 가능하면 표본 부족 문제가 상당히 완화된다

결과만 보고하고 크롤러 구현은 하지 않는다.

---

## 실행 순서 요약

```
G-0  스키마 확인                    → 완료
G-1  스키마 설계 + 마이그레이션      → 완료 (002_market_indicators.sql)
G-2  KOSPI/KOSDAQ/USD_KRW 적재      → 완료 (14,072행)
G-3  ECOS 지표 코드 조회            → 완료 (6종 확정, 반도체 수출금액지수 제외)
     ECOS 적재                      → 완료 (24,883행)
G-4  피처 조회 함수 + 누수 검증      → 완료 (feature_loader.py, get_features + verify_no_leakage)
G-5  (병행) 네이버 금융 과거 조회 범위 확인
```

**Task G(G-0~G-4) 전체 완료.** 정지 지점에서 임의 판단으로 다음 단계에 진입하지 않는다.

---

## 이 작업이 끝나면 가능해지는 것

1. **트랜스포머 ablation baseline** — 주가 + 거시지표만으로 학습. 감성 피처 자리는 비워둔다.
   이 성능이 "뉴스 감성을 추가해서 개선되었는가"를 증명할 기준선이 된다
2. **초과수익률 라벨링** — KOSPI 등락률이 확보되므로 Task E 착수 가능
3. `market_indicators` 미완료 항목 종결
