# 결과: Task T 변동성 예측 — 진행 경과 종합 (2026-09-06~07)

상위 문서: `결과_TaskT_방향예측_폐기.md`(방향 예측 폐기 배경, 상세) / `결과_변동성_조기경보_검증.md`
(⚠️ **버그 있던 버전 — 아래 [3] 참고, 폐기 대상**) / `CLAUDE.md`

목적: 2026-09-06~07 이틀에 걸쳐 진행된 변동성 예측 트랙 전체(전환 배경 → GARCH baseline →
치명적 라벨링 버그 발견·수정 → 재검증 → Parkinson 발견 → 하이브리드/원본시퀀스 실험 →
과적합·분산붕괴 진단 → 50종목 확장·검증 → 종목평균 제거 분해 검증 → 100종목 확장·최종
검증 → 자동화 파이프라인 작성)를 시간순으로 정리한다. **8종목 구간까지는 학습 기반 모델이
baseline을 넘지 못했으나, [10]~[11]의 50종목 확장으로 처음 통계적으로 유의한 개선이
확인됐고, [12]~[15]의 검증(종목평균 제거 분해, 100종목 확장, 표본 얇은 종목 재확인, target/
baseline 값 일치 재확인)을 거쳐 최종 결론으로 확정됐다** — 다만 walk-forward 재검증·실전
의사결정 백테스트는 다음 과제로 남아 "통계적으로 유의한 개선"이지 "실전 투입 가능 수준"은
아니다(아래 "최종 상태" 참고).

> ⚠️ **종목명 정정**: 이 문서와 `constants.py`에서 종목코드 `034730`은 처음부터 끝까지
> 일관되게 SK 주식회사(지주회사) 데이터였다. 확장 초기에 이 코드를 "현대건설"로 잘못
> 이름표를 붙였던 것을 50종목 확장 작업 중 발견해 정정했다(현대건설의 실제 코드는 000720,
> 별도 미등록). **가격 데이터·계산 결과 자체는 처음부터 정확했고 이름표만 틀렸던 것**이라
> 이 문서의 모든 034730 관련 수치는 그대로 유효하다.

---

## [1] 방향 예측 폐기 배경 (요약, 상세는 `결과_TaskT_방향예측_폐기.md`)

Task T-1(단일 종목) + 이번 세션(8종목 pooled + 종목 임베딩) 총 4번의 독립 평가에서 가격
방향(상승/하락) 예측이 전부 무작위 수준으로 확정됐다 — Random Walk/동전던지기/사전분포
무작위 예측기/레이블 셔플(N=7) 전부 통과 못함. 뉴스/감성 트랙(Task A~F)의 "신호 없음"
결론과 같은 성격이라 방향 예측 자체를 폐기하고 변동성(|수익률|) 예측으로 전환했다.

---

## [2] 변동성 예측 전환과 GARCH baseline 최초 발견 (⚠️ 버그 있던 수치, 아래 [3]에서 정정됨)

GARCH(1,1)(`arch` 패키지, 로그수익률×100, 종목별 독립 적합, train만으로 파라미터 추정 후
고정해 전체 구간 재귀 적용)을 SMA20(직전 20일 평균 |수익률|)과 비교하는 학계 표준 baseline
구축 첫 실행에서:

| | RMSE | MAE |
|---|---|---|
| GARCH(1,1) | (버그 버전) SMA 대비 약 14% 개선 | — |
| 하이브리드(GARCH σ를 15번째 피처로 추가한 Transformer) | GARCH 대비 42~43% 개선(8/8 종목) | — |

조기경보(급변 사전 감지) 검증도 이 버전으로 진행해 "84.6% recall, p=0.001, 국면 진입 첫날은
오히려 91.3%"라는 결과를 얻었다. **이 절의 수치는 전부 [3]의 버그로 인한 착시였음이 이후
밝혀졌다 — 역사적 기록으로만 남긴다.**

---

## [3] 라벨링 버그 발견과 수정

### 버그 내용

`가격예측/garch_baseline.py`의 `forecast_with_fixed_params()`(구 `fit_and_forecast_garch()`
내부 로직)가 `arch` 패키지의 `forecast(start=k, horizon=1)` 결과를 그대로 날짜 `k`에
라벨링하고 있었다. 실제로는 이 값이 **"k일까지의 데이터(k 자신의 수익률 포함)로 k+1일을
예측한 값"**이라 — 그날 자신의 등락폭이 이미 재료로 들어간, 명백한 미래정보 누수였다.

### 발견 경위

`가격예측_변동성_일일수집.py`(서빙용 "내일 예측" 함수) 작성 중, `forecast_next_day_sigma()`가
"오늘까지 데이터로 내일 예측"이 아니라 "이미 계산된 오늘 예측"과 동일한 값을 반환하는 것을
발견 → `arch`의 `forecast(start=k)` 의미를 재조사 → 수작업 GARCH 재귀식
(`σ²(t)=ω+αε²(t-1)+βσ²(t-1)`)과 실제 함수 출력을 위치별로 대조한 결과, 함수 출력이 "해석
A"(같은 위치)가 아니라 "해석 B"(한 칸 뒤 위치)와 소수점까지 정확히 일치함을 확인.

### 수정

`forecast_with_fixed_params()`가 원시 출력 행 i를 `start_pos+i+1` 날짜로 재라벨링하도록
수정(맨 마지막 값은 배열 밖 미래 시점이라 버림 — 서빙용은 `forecast_next_day_sigma()`가
별도로 이미 올바르게 처리). `fit_and_forecast_garch()`/`split_dataset.py`의 서빙 시퀀스
빌더도 이 변경에 맞춰 `start_pos`를 하나씩 당겨 호출하도록 동반 수정.

### 3중 독립 검증 (전부 실제값 대조, 코드 리뷰 아님)

1. **수동 재귀식 대조(005930)**: mu 보정 잔차로 손 계산한 σ가 수정된 함수 출력과 8자리까지
   완전 일치. 유일한 불일치(최대오차 0.27)는 계열 맨 앞 50개 날짜(2020-01 초, GARCH
   backcast 시딩 차이)에 국한 — train_end(2024-09-24)보다 4년 이상 앞이라 무관.
2. **다른 종목·다른 날짜 재확인**: 034730/2026-04-15(momentum·recent_vol_ma20·거시지표·
   target까지 전부), 051910/2026-06-01(GARCH σ만 별도) — 전부 원본 데이터부터 손으로
   재계산해 대조, 전부 일치.
3. **원본시퀀스 실험([7]) 착수 전 3종목 재확인**: 005930/2025-12-16, 105560/2026-04-02,
   035420/2026-07-15 — 20일 윈도우 전체(close_return/hl_range_ratio/open_gap_ratio/
   log_volume/garch_sigma)를 원본 OHLCV로 손 계산해 대조, 전부 일치, D 당일 값 섞임 없음.

(부수 발견: 거시지표(KOSPI 등)가 `build_stationary_features`의 사전 정렬 위에 프레임 전체가
한 번 더 shift(1)되어 최신값보다 하루 더 늦은 값을 쓰는 현상을 발견 — 재검토 결과 **리크는
아니고**(오히려 더 보수적) 2026-08-17 V2 설계 이후 계속 존재해온 특성으로, 오늘 새로 생긴
버그가 아니라 이미 안전한 것으로 판정됐던 사안임을 재확인했다. 최적화 여지는 있으나 이번
검증에서는 수정하지 않았다.)

---

## [4] 수정 후 재검증 결과

### GARCH(1,1) vs SMA20 (8종목, test n=236/종목)

| ticker | GARCH RMSE | GARCH MAE | SMA20 RMSE | SMA20 MAE | GARCH 승? |
|---|---|---|---|---|---|
| 005930 | 3.196002 | 2.399554 | 3.174026 | 2.332394 | 패 |
| 005380 | 3.191942 | 2.521502 | 3.125280 | 2.231593 | 패 |
| 051910 | 2.731912 | 2.105576 | 2.779425 | 2.073215 | 패 |
| 105560 | 1.863492 | 1.474358 | 1.805113 | 1.296545 | 패 |
| 207940 | 1.983879 | 1.524672 | 1.920853 | 1.295711 | 패 |
| 035420 | 2.521626 | 1.901033 | 2.628919 | 1.888449 | 패 |
| 034730 | 3.122351 | 2.339581 | 3.084722 | 2.272167 | 패 |
| 015760 | 2.403554 | 1.821128 | 2.461280 | 1.811533 | 패 |
| **평균** | **2.626845** | 2.010925 | **2.622452** | 1.900201 | **0/8** |

### 하이브리드(15피처: 14+GARCH σ) 재평가

| | RMSE | MAE |
|---|---|---|
| 순수 Transformer(14피처) | 2.865417 | 1.883791 |
| **하이브리드(15피처)** | **2.709781** | **1.833777** |
| GARCH(1,1) | 2.626845 | 2.010925 |
| SMA20 | 2.622452 | 1.900201 |

순수 Transformer 대비 개선(5.4%)은 있으나 **GARCH·SMA20 둘 다 못 이김**. 셔플(N=7) p=0.125
(이 N의 최솟값, 유의성 판정 불가 해상도).

### 정상/폭락 구간 분리 재확인 (005930 상세 + 8종목 요약)

| 구간 | GARCH RMSE | SMA20 RMSE | GARCH 승? |
|---|---|---|---|
| 평상시(115일) | 2.373 | 2.235 | 패 |
| 폭락구간(121일, 2026-02~07) | 3.817 | 3.860 | **승**(근소) |

8종목 요약: 평상시 승리 **0/8**, 폭락구간 승리 **4/8**(005930/051910/035420/015760) — "스트레스
구간에서 GARCH가 무너진다"는 가설과 **반대 방향**: GARCH는 β=0.85~0.93의 강한 지속성 때문에
평상시(변동 작음)에 오히려 SMA20보다 반응이 느려 손해를 본다.

### 조기경보 재검증 — 완전 붕괴

| | Recall | Precision | p-value |
|---|---|---|---|
| 버그 버전(1차 보고) | 84.6% | 72.6% | 0.001(유의) |
| **수정 후 재실행** | **0.7%** | 22.2% | **0.4186(유의 아님)** |

test 1,888건 중 모델 경보는 9건뿐(8종목 중 6종목 경보 0건). **조기경보 능력 자체가 버그로
만들어진 착시였음이 확정**됐다 — 2·3차(국면 진입 첫날, N 민감도) 재검증은 1차가 이미 우연
수준으로 무너져 실익이 없어 생략.

---

## [5] Parkinson 변동성 발견 — 현재까지 유일하게 검증된 개선

일중 고가-저가 범위 기반 Parkinson(1980) 추정량: `parkinson_vol(t) = sqrt(ln(high/low)²/(4ln2))×100`.

### 상관계수 (8종목 평균, test 구간, target=|당일 수익률|)

| | (a)당일 동시성(리크, 참고용) | (b)예측용(t-1까지) |
|---|---|---|
| Parkinson | 0.6418 | 0.2013 |
| GARCH σ | — | 0.2119 |
| SMA20 | — | 0.1683 |
| recent_vol_ma20 | — | 0.1679 |

### Parkinson-SMA20(20일 지연 이동평균) vs 기존 baseline (8종목 평균)

| | RMSE | MAE |
|---|---|---|
| **Parkinson-SMA20** | **2.5972** | **1.8773** |
| GARCH(1,1) | 2.6268 | 2.0109 |
| SMA20(종가기반) | 2.6225 | 1.9002 |

**GARCH·SMA20 둘 다(RMSE·MAE 모두) 이긴다** — 8종목 중 6종목에서 RMSE 기준으로도 SMA20보다
낫다. 이번 세션 전체에서 **유일하게 기존 baseline 둘 다를 실제로 넘어선 방법**이다.

---

## [6] Transformer가 이 신호를 못 씀

### SMA20을 16번째 피처로 추가 (recent_vol_ma20과 사실상 같은 정보임을 먼저 확인)

`recent_vol_ma20×100`과 GARCH baseline의 `SMA20`은 계산식(윈도우 20일, shift 정렬)이
동일하고 실측값도 ±1~2.4% 이내로 사실상 같은 값 — 즉 이 정보는 이미 15피처 하이브리드
안에 들어 있었다. 그런데도 SMA20 자체를 16번째 피처로 별도 추가해 재학습해도 개선되지
않았다(가설: 정규화가 항등매핑을 방해, 다른 피처들의 노이즈에 묻힘, garch_sigma와의 혼합
방식이 test에 일반화 안 됨).

### Parkinson-SMA20을 16번째 피처로 추가

중복도 확인(parkinson_ma20 vs recent_vol_ma20 상관 0.88, vs garch_sigma 0.65 — 0.95 미만,
"다른 정보"로 판단) 후 실제 재학습:

| | RMSE | MAE |
|---|---|---|
| 15피처 하이브리드 | 2.7098 | 1.8338 |
| **16피처(+parkinson_ma20)** | **2.7344** | **1.8356** |

**개선 없음, 오히려 악화**(8종목 중 6종목). GARCH·SMA20 대비도 개선 없음. 셔플(N=7) p=0.125.
**"단독 baseline으로는 이기는 정보라도 Transformer에 피처로 얹으면 못 쓴다"는 패턴이
SMA20에 이어 두 번째로 재현됐다.**

---

## [7] 원본 시퀀스 실험 — 압축 피처 제외해도 개선 안 됨

가설: "사람이 미리 압축한 통계(GARCH σ, SMA20 등)가 Transformer 고유의 시퀀스 패턴 학습을
방해했다." 검증: SMA20/recent_vol_ma20/Parkinson/momentum 등 압축 지속성 피처를 전부
빼고, `close_return/hl_range_ratio/open_gap_ratio/log_volume`(원본에 가까운 값) + GARCH σ
1개 + 거시지표 8개 + 종목 임베딩(13피처)만으로 재구성.

**사전 날짜매핑 검증(0단계, 실제값 대조)**: 8종목 중 3종목(005930/105560/035420)에서 각각
20일 윈도우 전체를 원본 OHLCV로 손 계산해 대조 — **전부 통과**, D 당일 값 섞임 없음, target·
GARCH σ 전부 일치.

### 결과 (8종목 평균)

| | RMSE | MAE |
|---|---|---|
| **원본시퀀스(13피처)** | **2.8397** | **1.8673** |
| 15피처 하이브리드 | 2.7098 | 1.8338 |
| GARCH(1,1) | 2.6268 | 2.0109 |
| SMA20 | 2.6225 | 1.9002 |
| Parkinson-SMA20 | 2.5972 | 1.8773 |

**8종목 전패 — 지금까지 시도한 모든 조합 중 가장 낮은 성능.** "압축 통계가 학습을
방해했다"는 가설은 **기각** — 오히려 반대로 원본에 가까운 값만 주니 더 나빠졌다.

---

## [8] 과적합 / 분산 붕괴 진단

### 학습 곡선 (MSELoss 기준)

**15피처 하이브리드** (best epoch=7):

| epoch | train_loss | val_loss |
|---|---|---|
| 1 | 2.2509 | 2.8243 |
| 3 | 2.1032 | 2.7908 |
| 7(best) | 2.0606 | 2.7789 |
| 15(조기종료) | 2.0421 | 2.8106 |

**13피처 원본시퀀스** (best epoch=12):

| epoch | train_loss | val_loss |
|---|---|---|
| 1 | 2.2511 | 2.8013 |
| 5 | 2.0971 | 2.7697 |
| 12(best) | 2.0507 | 2.7683 |
| 20(조기종료) | 2.0317 | 2.7722 |

train은 꾸준히(느리게) 감소, val은 epoch 3~7 근방에서 정체 — 1차 판정은 "과적합"이었다.

### train RMSE vs baseline TRAIN 성능 (in-sample)

| | train RMSE | train MAE | test RMSE | train→test 악화율 |
|---|---|---|---|---|
| 15피처 하이브리드 | 1.4182 | 0.9969 | 2.7820 | +96% |
| 13피처 원본시퀀스 | 1.4092 | 0.9974 | 2.9174 | +107% |
| GARCH(in-sample) | 1.6326 | 1.2901 | 2.6268 | +61% |
| SMA20(train) | 1.5654 | 1.0965 | 2.6225 | +68% |
| **"train 평균만 찍기"** | **1.478**(=√Var) | — | — | — |

모델이 GARCH·SMA20을 train에서 이기지만, **"train 평균만 찍기"(RMSE 1.478)조차 이미
GARCH·SMA20보다 낫다** — 모델의 실제 우위(1.418 vs 1.478)는 4%에 불과. 모델의 train→test
악화율(96~107%)도 baseline(61~68%)보다 커서 순수 레짐차이만으로는 설명 안 되는 부분이 있다.

### 분산 붕괴 확인 — 결정적 재진단

| 시점 | test 예측 mean | test 예측 std | 비고 |
|---|---|---|---|
| epoch0(무작위 초기화) | 0.2376 | 0.2270 | 노이즈 수준 |
| epoch1(1 epoch 학습) | 1.7856 | 0.2184 | — |
| **best_epoch(7)** | **1.8383** | **0.2608** | — |
| **실제 test target** | 2.7276 | **2.7453** | — |

- **best_epoch 예측 표준편차(0.26)가 실제 target 표준편차(2.75)의 9.5%** — 사실상 상수 찍기.
- epoch1↔best_epoch 예측 상관계수 **0.897** — 의미 있는 변화는 epoch 0→1 사이에서 다 끝남.
- epoch0(학습 전) loss=4.49/6.61 → **epoch1(1 epoch 후) loss=2.22/2.82로 이미 "train 평균만
  찍기" 수준(2.18)** — 첫 epoch 만에 대부분의 "학습"이 끝남.
- test에서 모델 MSE(7.953)는 "train 평균을 test에 그대로 찍기"(9.025)보다 겨우 12% 나을
  뿐이고, 그마저 대부분 "test 레짐이 더 변동성 크다"는 수준 정보(1.51→1.84로 예측 수준만
  이동)이지 날짜별 변화를 쫓아간 게 아니다.

**최종 재해석**: 정규화 과다로 인한 고전적 과적합(복잡한 train 패턴을 외워 test에서
못 씀)이 아니라, **MSE 손실이 약한 신호 아래에서 첫 epoch 만에 거의 상수(train 평균을 살짝
보정한 값)로 수렴해버리는 전형적 실패 패턴**이다. "train에서 baseline을 이긴다"는 것도
그 baseline들이 train에서는 평균-찍기보다도 못하다는 사실에 크게 기댄 것이라 증거력이 약함.

---

## [9] 다음 시도 후보 (미착수, 가설 단계)

- 분산 보존 손실 함수(예: MSE에 예측 분산 페널티 추가, 또는 quantile loss)
- 학습률/배치크기/모델 구조(d_model, 레이어 수, dropout) 재조정 — 지금 구조가 이 표본
  규모에 비해 최적인지 별도 검증 안 됨
- 분위수 회귀(quantile regression) — 변동성처럼 오른쪽 꼬리가 두꺼운 타겟에는 MSE 단일
  포인트 추정보다 분위수 예측이 나을 가능성
- 결정은 아직 하지 않았다 — 위는 선택지 나열이며 우선순위는 사람이 정함

---

## [10] 50종목 확장 — 표본 확대로 분산 붕괴 완화 시도

[8]의 분산 붕괴 진단 이후, "표본이 적어(8종목) 모델이 입력에 반응하는 법을 못 배운다"는
가설을 검증하기 위해 종목을 8→**50개**로 확장했다.

- `fdr.StockListing('KOSPI')`로 전체 KOSPI 종목 조회 → 종목명이 "우"/"우B"/"우C" 등으로
  끝나는 우선주 필터링(109개 제외) → 시가총액(`Marcap`) 상위 50개 선정
- 기존 9종목(활성 8 + SK하이닉스)이 전부 상위 50위 안에 이미 포함됨을 확인. 000660(SK
  하이닉스)도 이번엔 활성화(50종목 규모에서는 섹터 분산보다 표본 확대가 우선이라 판단)
- **종목명 오류 발견·정정**: 034730이 "현대건설"로 잘못 등록돼 있었음을 발견, 실제로는
  SK 주식회사(지주)임을 확인해 정정(위 상단 경고 박스 참고)
- 41개 신규 종목 OHLCV 적재(`주가_초기적재.py` 재사용, `STOCK_INITIAL_LOAD_START`부터).
  47개는 2020-01-02~2026-09-04 전체 커버, **3개는 상장/재상장일이 늦어 이력이 짧음**
  (LG에너지솔루션 2022-01-27~, SK스퀘어 2021-11-29~, HD현대중공업 2021-09-17~) — 셋 다
  val_end(2025-09-15)보다 훨씬 이전에 상장돼 **val/test는 50종목 전부 동일(237/236개)하고
  train만 자연스럽게 적게 확보됨**. `pooled_dataset.py`가 애초에 이 상황을 염두에 두고
  설계돼 있어(주석에 명시) 코드 수정 없이 정상 동작 확인(드라이런: train 52,838 / val
  11,850 / test 11,800, 에러 없음)
- 종목 임베딩 차원을 8 → **16**으로 확대(50종목 규모에서 8은 좁고, d_model=32 대비 임베딩이
  절반을 넘지 않도록 절충)

---

## [11] 50종목 결과 — 오늘 세션 유일의 통계적으로 유의한 개선

### 분산 붕괴 완화

| | 예측 std | target std | 비율 |
|---|---|---|---|
| 8종목(이전, [8]) | 0.2608 | 2.7453 | 9.5% |
| **50종목** | **0.9272** | 3.0110 | **30.8%** |

예측 범위도 0.29~4.09로 넓어짐(이전 1.00~2.45) — 완전히 해소되진 않았으나 뚜렷한 개선.

### baseline 비교 (50종목 평균)

| | RMSE | MAE |
|---|---|---|
| **하이브리드(50종목, 15피처)** | **2.7669** | **1.8985** |
| GARCH(1,1) | 2.8002 | 2.1061 |
| SMA20 | 2.7853 | 1.9984 |
| Parkinson-SMA20 | 2.7463 | 1.9674 |

GARCH·SMA20 대비 개선(RMSE·MAE 모두) = **True**. Parkinson-SMA20 대비는 근소하게 미달
(RMSE −0.7%, MAE는 오히려 +3.5% 앞섬 — 사실상 대등).

### 셔플 검증 — N=7 → N=30 확장

| | 셔플 평균±표준편차 | 실제값 | 격차(σ) | p-value |
|---|---|---|---|---|
| RMSE, N=7 | 3.1866±0.0097 | 2.8775 | 31.8σ | 0.1250(해상도 한계) |
| **RMSE, N=30** | 3.1875±0.0133 | 2.8775 | 23.3σ | **0.0323** |
| MAE, N=7 | 2.0124±0.0016 | 1.8985 | 69.6σ | 0.1250(해상도 한계) |
| **MAE, N=30** | 2.0124±0.0024 | 1.8985 | 47.7σ | **0.0323** |

30/30 셔플 전부 실제 모델보다 나쁨. N을 늘려도 격차(20~50σ 규모)가 무너지지 않고 견고하게
유지됨 — N=7에서의 p=0.125는 착시가 아니라 표본크기 해상도 한계였을 뿐이었고, N=30에서
**p<0.05로 통계적 유의성이 명확히 확인**됐다.

---

## [12] 종목평균 제거(demean) 분해 검증 — "종목 간 차이만 맞힌 것 아니냐"는 우려 확인

**우려**: 50종목 확장 후 예측 분산이 커진 것([11])이 "종목마다 평균 변동성 수준이 다르다"는
사실만 반영한 것이고, 정작 "그 종목이 오늘 유독 더 흔들릴지" 날짜별 타이밍은 못 맞히는
것 아니냐는 지적. 확인 방법: 각 종목의 실제/예측 평균을 각각 빼(demean) 종목 고유 수준
차이를 걷어낸 잔차끼리 다시 RMSE/MAE/상관계수를 계산 — baseline 3종에도 동일하게 적용해
"원본(raw)"과 "종목평균 제거(demean)" 두 지표를 나란히 비교했다.

### (a) 원본 vs (b) 종목평균 제거 — 상관계수·RMSE·MAE (50종목, 공통 정렬 표본 n=11,800)

| method | r(raw) | r(demean) | RMSE(raw) | RMSE(dm) | MAE(raw) | MAE(dm) |
|---|---|---|---|---|---|---|
| **hybrid** | 0.3434 | **0.2556** | 2.8775 | 2.8051 | 1.8985 | 1.9587 |
| garch | 0.3275 | 0.2345 | 2.8951 | 2.8499 | 2.1061 | 1.9844 |
| sma20 | 0.3149 | 0.2021 | 2.8869 | 2.8864 | 1.9984 | 2.0069 |
| parkinson_sma20 | 0.3339 | 0.2383 | 2.8413 | 2.8284 | 1.9674 | 1.9520 |

### baseline 대비 하이브리드 우위 — 원본 vs 종목평균 제거 후

| | vs GARCH | vs SMA20 | vs Parkinson-SMA20 |
|---|---|---|---|
| (a) 원본 RMSE | 개선 | 개선 | **역전당함**(2.8775 vs 2.8413) |
| (a) 원본 MAE | 개선 | 개선 | 개선 |
| (b) 종목평균 제거 RMSE | 개선 | 개선 | **개선**(2.8051 vs 2.8284, 역전 사라짐) |
| (b) 종목평균 제거 MAE | 개선 | 개선 | 개선 |

**핵심 발견**: Parkinson-SMA20 대비 원본 RMSE에서 근소하게 졌던 것([11]의 "근소 미달")이
**종목평균을 제거하고 나면 사라진다** — 그 근소한 열세가 "종목 간 평균 수준 차이"였을 뿐,
날짜별 순수 예측력에서는 애초에 하이브리드가 앞서 있었다는 뜻이다.

### 판정

하이브리드 상관계수는 원본 r=0.3434 → demean r=0.2556로 하락(약 25% 상대 감소)하지만
0에 가깝게 무너지지 않고 **유의미한 수준으로 유지**된다. 종목 간 차이를 완전히 걷어낸
뒤에도 GARCH·SMA20 대비 우위는 그대로이고, Parkinson-SMA20 대비는 오히려 더 확실해진다.
**"분산 확대가 종목 간 평균 차이일 뿐"이라는 우려는 기각** — 종목 고유 수준을 제거한
순수 날짜별 변동에서도 baseline보다 나은 예측력이 확인됐다.

(주: 이 검증은 상관계수/RMSE 분해만 수행 — 유의성(셔플) 검증은 [11]의 N=30 결과를 그대로
원용하며 이 단계에서 별도 재검증하지 않았다. 스크립트: `가격예측/test_evaluation_pooled50_decompose.py`.)

---

## [13] 100종목 확장 — 두 개선 효과가 표본을 늘려도 이어지는지 확인

[11]~[12]에서 확인된 두 효과(분산 붕괴 완화, 종목평균 제거 후에도 살아있는 예측력)가
50→100종목으로 더 늘려도 이어지는지 "방향 확인" 목적으로 재검증했다(완벽한 통계적 확정이
아니라 셔플 N=5로 축소 — 아래 참고).

### 종목 확장

`fdr.StockListing('KOSPI')` 재조회, 동일한 우선주 필터(109개 제외)로 시가총액 상위 100위
산정 — 기존 50종목 전부 포함됨을 확인. 상위 51~100위 후보 중 3종목은 momentum 60일
lead-in을 감안하면 train 표본이 0~38행 수준으로 지나치게 얇아(임베딩이 사실상 학습되지
않을 위험) 제외하고 다음 순위로 대체했다:

| 제외 | 사유 | 대체 |
|---|---|---|
| 0126Z0(삼성에피스홀딩스) | 상장 2025-11-24, train 0행/val 0행(전 표본이 test 구간) | 036570(NC) |
| 064400(LG씨엔에스) | 상장 2025-02-05, train 0행 | 088980(맥쿼리인프라) |
| 062040(산일전기) | 상장 2024-07-29, train 38행 | 052690(한전기술) |

대체 3종목은 전부 2020-01-02부터 전체 이력 보유. 신규 47종목(대체 3종목 포함 50종목 중
가격 데이터가 이미 있던 것 제외) OHLCV 적재 완료.

### 예측 분산 붕괴 완화 추이 — 8→50→100종목

| 종목 수 | 예측 std / target std | demean 상관계수 r |
|---|---|---|
| 8종목 | 9.5% | — (당시 미측정) |
| 50종목 | 30.8% | 0.2556 |
| **100종목** | **33.4%** | **0.2795** |

두 지표 모두 확장할수록 계속 개선되는 방향 — 100종목에서도 추세가 꺾이지 않았다.

### baseline 3종 비교 (100종목, 종목별 독립계산 평균)

| | RMSE | MAE |
|---|---|---|
| **하이브리드(100종목)** | **2.7406** | **1.8681** |
| GARCH(1,1) | 2.8280 | 2.1172 |
| SMA20 | 2.7826 | 1.9822 |
| Parkinson-SMA20 | 2.7516 | 1.9742 |

**GARCH·SMA20·Parkinson-SMA20 전부 개선** — 50종목([11])에서 근소하게 못 넘었던
Parkinson-SMA20도 이 표본 기준으로는 넘겼다.

### (a) 원본 vs (b) 종목평균 제거 — 상관계수·RMSE·MAE (100종목, 공통 정렬 표본 n=23,650)

| method | r(raw) | r(demean) | RMSE(raw) | RMSE(dm) | MAE(raw) | MAE(dm) |
|---|---|---|---|---|---|---|
| **hybrid** | 0.3787 | **0.2795** | 2.8899 | 2.8295 | 1.8681 | 1.9313 |
| garch | 0.3385 | 0.2279 | 2.9619 | 2.9087 | 2.1172 | 1.9768 |
| sma20 | 0.3397 | 0.2131 | 2.9196 | 2.9189 | 1.9822 | 1.9905 |
| parkinson_sma20 | 0.3561 | 0.2442 | 2.8825 | 2.8708 | 1.9742 | 1.9520 |

이 공통 정렬 표본(baseline 4종 가용일 교집합) 기준으로는 vs Parkinson-SMA20 원본 RMSE만
근소하게 역전당하지만(2.8899 vs 2.8825, [12]와 동일 패턴), **종목평균 제거 후에는 세
baseline 전부(RMSE·MAE 모두) 이긴다.**

### 셔플 검증 N=5 — "방향 확인"용(통계적 확정 목적 아님)

| 지표 | 셔플 평균±표준편차 | 실제값 | 격차(σ) | p-value |
|---|---|---|---|---|
| RMSE(원본) | 3.2508±0.0038 | 2.8899 | **96.1σ** | 0.1667(N=5 해상도 한계) |
| MAE(원본) | 2.0146±0.0014 | 1.8681 | **107.5σ** | 0.1667 |
| RMSE(종목평균제거) | 2.9466±0.0010 | 2.8295 | **121.3σ** | 0.1667 |
| MAE(종목평균제거) | 2.0361±0.0009 | 1.9313 | **116.5σ** | 0.1667 |

5개 셔플 전부 실제 모델보다 뚜렷하게 나쁨(0/5). p-value는 N=5의 해상도 한계(1/6)에
걸려있지만, 격차가 90~120표준편차로 노이즈로 설명 가능한 수준을 압도적으로 넘는다 —
"방향 확인"이라는 목적에는 충분한 근거.

(스크립트: `가격예측/test_evaluation_pooled100_hybrid.py`. EMBEDDING_DIM을 50종목 시절
16에서 20으로 소폭 확대, d_model=32는 50종목 검증과 동일하게 유지해 비교 가능성을 지켰다.)

---

## [14] 443060 / 278470 얇은 표본 재확인 — 재집계 불필요 판정

[13]에서 100종목에 편입된 신규 상장 종목 중 특히 얇았던 443060(HD현대마린솔루션)과
278470(에이피알)의 실제 train 시퀀스 수를 momentum 60일 lead-in + lookback 20을 반영해
직접 세어 확인했다(재학습 없이 조회/재집계만).

| 종목 | merged 전체 행(dropna 후) | lookback=20 후 전체 시퀀스 | **train** | val | test |
|---|---|---|---|---|---|
| 443060 | 506 | 487 | **13개** | 237개 | 237개 |
| 278470 | 553 | 534 | **60개** | 237개 | 237개 |

443060은 두 자릿수지만 여전히 얇고(momentum 60일이 494일 이력 중 상당 부분을 소모), 278470은
우려했던 것보다 여유가 있었다. **둘 다 "5개 미만" 기준에는 해당하지 않아, 두 종목을 제외한
100종목 재집계는 수행하지 않았다** — 애초에 이번 실행이 종목별 원시 예측 배열을 저장하지
않아 재집계하려면 최소 1회 재추론이 필요했을 것이나, 그 필요 자체가 조건 미충족으로 사라졌다.

---

## [15] target-baseline 값 일치 재확인

[12]의 decompose 검증이 하던 "하이브리드 target과 baseline realized 값이 실제로 같은가"
교차검증을 [13]의 100종목 통합 스크립트에서는 빠뜨렸던 것을 뒤늦게 별도로 재확인했다
(수식상 동일함은 코드 대조로 이미 확인했지만, 실측 값 대조는 하지 않은 채였음).

005930 / 443060 / 278470 각 5개 test 날짜(총 15건)에서 `merged['target']`(하이브리드가
학습에 실제로 쓰는 값, `|log(close_t/close_(t-1))×100|`)과 `compute_log_returns_pct(close).abs()`
(baseline이 쓰는 realized 값)를 직접 비교 — **15건 전부 소수점 10자리까지 완전히 일치,
max_diff = 0.00e+00.** 두 계산 경로(`시장지표.feature_loader.get_features()`의 raw close
기반 vs `garch_baseline.load_close_prices()`의 raw close 기반)가 실제로 동일한 값을 만든다는
것을 실측으로 최종 확인했다.

---

## 최종 상태 (2026-09-07 기준, 확정)

**100종목 pooled 하이브리드 모델이 GARCH·SMA20·Parkinson-SMA20 세 baseline을 원본
지표와 종목평균 제거(demean) 지표 양쪽에서 통계적으로 유의하게 이겼다.** 8종목에서
"첫 epoch 만에 거의 상수로 수렴하는 분산 붕괴"([8])로 실패했던 것이, 50→100종목으로
표본을 늘리며 분산 붕괴가 계속 완화되고(std 비율 9.5%→30.8%→33.4%) baseline 개선폭도
넓어졌다([10]~[11]). "분산 확대가 종목 간 평균 차이일 뿐"이라는 우려도 종목평균 제거
분해 검증으로 기각됐다([12]~[13], demean r 0.2556→0.2795로 오히려 개선). 100종목
검증에서는 원본 지표 기준으로 GARCH·SMA20·Parkinson-SMA20 셋 다 처음으로 전부 이겼고,
N=5 셔플 검증에서도 격차가 90~120표준편차로 노이즈 수준을 압도했다([13]). 신규 편입된
얇은 표본 종목([14])과 target/baseline 값 정의 일치([15])도 별도로 재확인해 결과의
신뢰도를 보강했다.

**정직한 한계**(이 결과를 과대해석하지 않기 위해 명시):
- **demean r=0.2795** — 결정계수 R²≈0.078. 종목 고유 수준을 걷어낸 뒤 "그 종목이 오늘
  유독 흔들릴지" 날짜별 타이밍을 맞히는 설명력은 약 7.8%에 불과하다. 통계적으로 유의한
  것과 실질적으로 강력한 것은 다르다.
- **극단치 예측이 약하다** — 100종목 test 예측 분포는 min=0.16/max=4.52인데 실제 target은
  min=0.00/max=26.24까지 간다. 예측 std가 target std의 33%뿐이라는 것도 결국 "가장 중요한
  급변 구간을 여전히 못 잡는다"는 의미다. 변동성 예측을 실전에서 쓰는 이유(리스크 관리,
  포지션 조정)가 정확히 이 지점인데, 이 모델이 가장 약한 지점이기도 하다.
- **walk-forward 미검증** — 지금까지 전부 고정된 단일 70/15/15 분할 하나로만 검증했다.
  여러 시점에 걸쳐 반복 검증한 적이 없어, 이번 test 구간(2025-09~2026-09)에 특화된
  결과일 가능성을 배제하지 못한다.
- **실제 의사결정 백테스트 없음** — 거래비용/슬리피지를 반영한 백테스트가 전혀 없다.
  RMSE가 baseline보다 몇 % 낮다는 것이 실제 의사결정(포지션 사이징 등)에 옮겨질 때
  이득이 되는지는 검증되지 않았다.

**결론**: **통계적으로 유의한 개선이지, 실전 투입 가능 수준은 아직 아니다.** 졸업작품
차원에서 "baseline 대비 유의한 개선을 보였다"는 주장은 100종목 검증까지 마친 지금
충분히 방어 가능하다 — 다만 그 이상(실제 트레이딩 보조 신호로서의 실용성)을 주장하려면
위 네 가지 한계를 먼저 해소해야 한다.

**자동화 파이프라인**: 100종목 검증 결과를 그대로 운영에 옮기는 코드
(`가격예측/가격예측_변동성_공통.py`, `가격예측/가격예측_변동성_일일수집.py`,
`가격예측/종목_월간갱신.py`)를 작성 완료했다 — 배포 게이트는 GARCH·SMA20·Parkinson-SMA20
세 baseline 전부를 이겨야 통과로 확정했다. **코드 작성만 완료, 실행/스케줄러 등록은
아직 하지 않았다.** 상세는 `CLAUDE.md` "파이프라인 실행 순서" 및 "다음 세션 시작 시 할
일" 참고.
