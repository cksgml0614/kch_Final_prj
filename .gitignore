# 환경변수 / 비밀정보
.env

# 가상환경
.venv/

# DB 데이터
postgres_data/

# Python
__pycache__/
*.pyc

# IDE
.idea/

# 로그
*.log

# KoBERT 학습 체크포인트/학습곡선 (대용량 바이너리, 로컬에서 생성)
# 2026-09-12: 뉴스/감성/라벨 트랙 쓰래기통 이동에 따라 경로 갱신
쓰래기통/감성분석/checkpoints/

# Task T 진단/학습 산출물 (히트맵 등 이미지, 로컬에서 생성)
가격예측/checkpoints/

# search_backfill 진행 체크포인트 (재개용 상태 파일, 로컬에서 생성)
# 2026-09-12: 뉴스/감성/라벨 트랙 쓰래기통 이동에 따라 경로 갱신
쓰래기통/뉴스데이터/checkpoints/

# BigKinds 원본 CSV (대용량 데이터 파일, 로컬에서 직접 배치)
빅카인즈_뉴스/

# MLflow 로컬 트래킹 DB + 아티팩트 저장소 (로컬에서 생성, 2026-09-14)
mlflow.db
mlflow_artifacts/

# daily_pipeline.bat 통합 실행 로그 (프로젝트 루트, 2026-09-14 신규) — *.log 규칙과
# 별개로 명시: 종목별 진단 로그가 쌓이는 가격예측/logs/와 헷갈리지 않게 경로를 구분
/logs/
