# 결과: Task T 방향 예측 폐기

작성일: 2026-09-06
상위 문서: `TASK_T_transformer_baseline.md`(Task T-1 지시서, 완료 아카이브) / `CLAUDE.md`
목적: 가격 방향(상승/하락) 예측을 목표로 진행했던 Task T-1(단일 종목) + 이번 세션(pooled
다종목)의 전체 시도와 실패 판정 근거를 기록하고, 왜 방향 예측을 접고 변동성 예측으로
전환했는지 남긴다. 변동성 예측(GARCH baseline, 하이브리드 Transformer, 조기경보 검증)은
`결과_변동성_조기경보_검증.md` 및 관련 문서 참고 — 이 문서는 "폐기된 방향 예측 트랙"만 다룬다.

---

## [1] 시도한 것 — 단일 종목 (Task T-1, 2026-08-17 완료)

- V1(레벨 기반 피처) → 비정상성 문제 발견(train/test 가격 레벨 괴리) → V2(비율 기반 피처:
  close_return/hl_range_ratio/open_gap_ratio/log1p(volume)/국고채 스프레드)로 재설계
- 진단 체인: 선형회귀 baseline, 다중공선성, Transformer 소형/기존 규모 비교, 하락 클래스
  precision/recall 게이트, 7시드 재현성, PR curve vs 무작위
- **최종 결론(Task T-1)**: "가격+거시지표만으로는 익일 방향성이 무작위 수준과 구분 안 됨"
  — val 기준. test 평가는 뉴스 트랙 결합 여부 결정 대기로 보류돼 있었음.

## [2] 시도한 것 — V2 최초 test 평가 (2026-09-06, 이번 세션)

뉴스/감성 트랙(Task F)이 "신호 없음"으로 완전히 종료되면서 "뉴스 결합 대기"라는 보류 사유
자체가 소멸 — V2 test 평가를 처음으로 실행했다.

| 지표 | 실제 모델 | Random Walk baseline(예측=0) |
|---|---|---|
| RMSE | 0.048819 | 0.048698 |
| MAE | 0.035801 | 0.035350 |
| 방향성정확도 | 47.46% | 50%(동전던지기) |

- Random Walk baseline을 못 이김(RMSE/MAE 둘 다 악화)
- 방향성정확도(47.46%)가 50% 동전던지기보다도 낮음(이항검정 p=0.474, 유의하지 않지만 방향이
  더 나쁨)
- train 상승비율 인지 무작위 예측기(49.20%)보다도 낮음
- 레이블 셔플(N=7) 검증: MAE 기준 7/7 셔플이 실제보다 우수(p=1.000), RMSE 기준 5/7(p=0.750)
- **판정: test에서도 val 결론(무작위 수준)이 재현됨 — 오히려 더 나쁨**

## [3] 시도한 것 — 다종목 pooled + 종목 임베딩 (2026-09-06, 이번 세션)

표본 부족(단일 종목 test 236일)을 극복하기 위해 8종목(005930/005380/051910/105560/
207940/035420/034730/015760)을 `PooledTransformerRegressor`(종목 임베딩 concat 방식)로
동시 학습 — pooled test 표본 1,888개(236×8)로 8배 확대.

| 기준 | 결과 |
|---|---|
| Random Walk baseline 개선 | False |
| 50% 동전던지기 대비 유의 | False (오히려 유의하게 낮음, p=0.005) |
| 사전분포 무작위 예측기 대비 유의 | False |
| 레이블 셔플(N=7) 대비 유의 | False (7/7 완패) |

8종목 전부 "무작위 수준"으로 개별 판정됐고 다중비교로 우연히 통과한 종목도 없었다.
**표본을 늘리고 cross-sectional 정보(종목 임베딩)를 추가해도 결론이 바뀌지 않았다.**

## [4] 폐기 사유 종합

1. 단일 종목(val+test), pooled(val+test) — 4번의 독립적 평가 전부 동일한 결론
2. 매번 baseline(RW/동전던지기/사전분포)뿐 아니라 **레이블 셔플 검증까지 포함한 엄격 기준**을
   적용했고, 전부 실패
3. 뉴스/감성 트랙(Task A~F)도 동일한 성격의 결론("신호 없음")에 도달해 있어 — 가격 방향
   예측이라는 문제 자체가 이 데이터·이 기간 범위에서 원리적으로 어렵다는 정황이 두 트랙에서
   교차 확인됨(효율적 시장 가설과 정합적인 결과)
4. 반면 같은 데이터로 **문제를 "변동성 예측"으로 재정의**하자 GARCH(1,1) baseline이 SMA20을
   뚜렷이 이기고(8종목 평균 RMSE 14% 개선), 여기에 Transformer를 하이브리드로 결합하니
   GARCH까지 압도적으로 이겼다(RMSE 42~43% 개선, 8/8 종목) — 상세는
   `결과_변동성_조기경보_검증.md` 및 CLAUDE.md 참고

## [5] 결정

**Task T의 방향(상승/하락) 예측 트랙을 폐기하고, 변동성(|수익률|) 예측으로 완전히 전환한다.**

폐기된 파일(2026-09-06, `쓰래기통/`으로 이동):
- `가격예측_공통.py` (단일 종목 방향 예측 운영 파이프라인 공용 로직)
- `가격예측_일일수집.py` (단일 종목 방향 예측 일일 실행 진입점)
- `test_evaluation_방향예측.py` (구 `test_evaluation.py`, 단일 종목 방향 예측 V2 test 평가)

보존된 파일(운영 대상 아님, 재실행 금지 — 역사적 기록 + 하이퍼파라미터 참조용):
- `가격예측_통합모델.py`(pooled 방향 예측) — 파일 정리 목록에서 최종 삭제 여부 별도 검토
- `test_evaluation_pooled.py`(pooled 방향 예측 test 평가)
- Task T-1 진단 체인 전체(`diagnose_baseline.py` 등) — 기존과 동일하게 기록으로만 보존

새 운영 파이프라인은 `가격예측_변동성_일일수집.py`(2026-09-06 신설)이며, `model_predictions`
테이블도 변동성 예측 스키마로 완전히 교체됐다 — 상세는 CLAUDE.md 참고.
