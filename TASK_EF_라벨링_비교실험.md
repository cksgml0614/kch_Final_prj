# 작업 지시서: Task E/F — 라벨링 및 비교 실험

작성일: 2026-08-29
선행 문서: `TASK_개정판_데이터_재구축.md`(Task A~D, 완료 아카이브)
정본: 이 문서는 CLAUDE.md의 D-1/D-2/D-3/D-7/D-8/D-9/D-10을 정본으로 삼아 작성됐다. 옛
`TASK_개정판_데이터_재구축.md`의 Task E/F 문면(D-2가 금지한 분위수 임계값을 지시하는 등,
D-3의 옛 정상 레짐/스트레스 2분할 방침 포함)은 낡은 내용이므로 참고하지 말 것.

**2026-08-30 실행 전 검토 반영**: 아래 4곳이 이번 개정으로 바뀌었다 — (1) 변동성 2-class
임계값 후보 명시(기존엔 값 없이 "임계값"만 표기), (2) 라벨 저장 방식을 뉴스 테이블 컬럼 추가에서
별도 테이블(`daily_labels`)로 변경, (3) D-3 개정(스트레스 구간 분리 폐기, 전체 기간 사용) 반영,
(4) 휴장일 뉴스 귀속 규칙 신설.

**착수 조건**: 뉴스 백필(search_backfill) 완료 후. 진행률/재개 방법은 CLAUDE.md 로드맵의
"뉴스 백필 진행 상황" 절 참고.

---

## 데이터 소스 정책 (2026-08-29 결정)

`daily_news`(크롤링, search_backfill)를 학습·운영 **정본**으로 삼는다. `daily_news_bigkinds`
(BigKinds)는 매일 자동 수집이 불가능(유료, 수동 다운로드)해 MLOps 자동화 요건을 충족하지
못하므로 정본이 될 수 없다.

`daily_news_bigkinds`는 다음 두 목적의 **실험 자산**으로 사용한다:
- (a) 데이터 소스 품질 비교
- (b) **과거 데이터를 빅카인즈로 대체 가능한지 검증** — 대체가 검증되면 향후 종목·기간 확장
  시 초기 학습 데이터 확보 비용이 크게 줄어든다

---

## Task E — 라벨 생성

### 절차 (CLAUDE.md D-2 그대로 적용, 변형 금지)

```
excess_return_t = 종목 change_rate_t − KOSPI change_rate_t
sigma_t         = 직전 N거래일 excess_return 표준편차 (t 시점 미포함)
z_t             = excess_return_t / sigma_t
```

- `sigma_t`는 t-1까지의 데이터만 사용
- 윈도우 N은 20/60/120을 비교해 클래스 분포 안정성을 보고
- 초기 N일은 라벨 NULL

### ⚠️ 분위수 기반 임계값 금지

CLAUDE.md D-2가 **미래 정보 누수**를 근거로 명시적으로 기각한 방식이다: "전체 기간 분위수로
임계값을 정하면 test 구간 분포 정보가 라벨 정의에 유입된다." 절대 분위수 기반 임계값을 쓰지
말 것 — 이 문서만 보고 실행하는 사람이 나중에 되돌리지 않도록 여기 다시 명시한다.

### 임계값 결정 — 판단 정지 지점

z 분포를 출력한 뒤 최종 임계값은 **사람이 결정**한다. 아래 표의 후보값(방향 5-class/3-class,
변동성 2-class 전부)은 CLAUDE.md D-2가 제시한 것이며, 어느 것도 미리 확정하지 말고 z 분포·클래스
분포와 함께 제시한 뒤 정지할 것.

### 라벨 저장 방식 — 별도 테이블 `daily_labels` (2026-08-30 결정, 뉴스 테이블 컬럼 추가 방식 폐기)

라벨을 뉴스 테이블(`daily_news`/`daily_news_bigkinds`)에 **넣지 않는다.** 라벨은 `(ticker,
date)` 단위인데 뉴스 테이블에 넣으면 같은 날 기사 수만큼 같은 값이 중복 저장되고, 임계값을
바꿀 때마다 두 뉴스 테이블을 모두 갱신해야 해서 관리가 어려워진다.

대신 새 테이블 `daily_labels`를 만든다:

| 컬럼 | 설명 |
|---|---|
| `ticker, date, window_n` | PK(복합키). `window_n`은 20/60/120 중 하나 — 세 윈도우를 한 테이블에 함께 보관 |
| `excess_return` | 종목 change_rate − KOSPI change_rate |
| `sigma` | 직전 `window_n`거래일 excess_return 표준편차(t 시점 미포함) |
| `z_score` | `excess_return / sigma` |

**라벨(5-class/3-class/2-class) 자체는 저장하지 않는다.** `z_score`만 저장해두고 조회 시
임계값을 적용해 파생한다 — 임계값을 바꿔도 재계산이 필요 없고, 여러 임계값 조합을 자유롭게
실험할 수 있다.

학습 시에는 `daily_labels`를 `daily_news`/`daily_news_bigkinds`와 `(ticker, date)`로 조인한다.
뉴스 테이블에는 라벨 컬럼을 추가하지 않는다. 기존 `sentiment_score`(daily_news 컬럼)는 D-1에
따라 그대로 보존하되(비교 기록용, 덮어쓰기 금지) 신규 라벨과는 무관한 별개 컬럼으로 둔다.

`Database/뉴스/` 등 기존 컨벤션대로 `Database/라벨/`(폴더명은 구현 시 확정) 하위에
`테이블_생성.sql`/`데이터_삭제.sql`/`테이블_삭제.sql` 3종을 **Task E 구현 시** 작성한다. 이
문서 개정 시점 기준 **아직 작성되지 않았다 — 설계만 확정된 상태**다. 스키마 요약은 CLAUDE.md
DB 스키마 절에도 반영돼 있다.

### 라벨 정의 — 5-class / 3-class / 2-class

**라벨 3종(방향 5-class·3-class, 변동성 2-class)을 함께 파생 가능하도록 `z_score`를
계산**한다 — z만 있으면 조회 시점에 셋 다 파생되므로 추가 저장 비용이 없다:

| 라벨 | 클래스 | 임계값 후보 |
|---|---|---|
| 방향 | 5-class | z ±0.5 / ±1.5 (부호 포함) |
| 방향 | 3-class | z ±0.5 (부호 포함) |
| 변동성 | 2-class | \|z\| > **0.5 / 1.0 / 1.5 (후보 3종)** (부호 없이 강도만) |

방향 라벨과 동일하게, 변동성 2-class도 **후보를 하나로 미리 고정하지 않는다.** 세 후보
(\|z\|>0.5/1.0/1.5) 전부에 대해 클래스 분포를 함께 계산·출력하고, 최종 임계값은 위 "임계값
결정 — 판단 정지 지점"과 같은 원칙으로 사람이 분포를 보고 결정한다.

근거: 선행연구상 뉴스는 방향보다 변동성/강도를 더 안정적으로 예측한다는 결과가 반복적으로
보고된다. 어느 쪽에 실제로 신호가 있는지는 미리 정하지 않고 Task F 게이팅에서 데이터로
판정한다.

### KOSPI 데이터

**재수집 불필요.** Task G-2에서 이미 `market_indicators`에 적재 완료됐다. ⚠️ 실제 저장된
`indicator_code`는 `KS11`이 아니라 **`KOSPI`**다(CLAUDE.md의 "코드 명명 규칙" 문서와 실제 DB가
달랐던 부분 — 2026-08-29 수정됨). `시장지표/feature_loader.py`로 읽기만 할 것. 재수집 코드를
새로 작성하지 않는다.

### 학습 기간 — 전체 기간 사용 (D-3 개정 반영, 2026-08-30)

CLAUDE.md D-3 개정에 따라 **정상 레짐/스트레스 구간 2분할·스트레스 구간 제외 방침은
폐기됐다.** Task E/F는 `constants.py`의 `TRAIN_PERIOD_START`/`TRAIN_PERIOD_END`로 정의된 전체
기간을 그대로 사용해 D-8 방식(시계열 순서 70/15/15, `SPLIT_RATIOS` 상수)으로 분할한다. 별도
스트레스 구간 홀드아웃이나 종목별 자동 탐지·제외 로직을 만들지 않는다.

개정 사유는 CLAUDE.md D-3 참고(요약: 종목 확장 시 종목마다 다른 스트레스 구간을 매번
탐지·제외해야 해 자동화가 복잡해짐 / 실제 운영에서는 스트레스 구간 시작을 사전에 알 수 없어
사후 판별 정보로 학습 데이터를 거르는 것은 미래 정보 누수 / 롤링 z-score가 레짐 변화를 흡수함
/ 스트레스 구간 성능 저하는 제거할 오염이 아니라 D-4가 다룰 분석 결과).

**단, D-8의 split별 레짐 특성 3종 보고는 그대로 필수다** — test 구간이 스트레스 기간을 포함
한다는 사실이 숫자로 드러나야 아래 "보고 항목"의 결과 해석이 가능하다.

### 뉴스 라벨 매칭 규칙 — D일 뉴스는 D보다 뒤인 첫 거래일에 귀속 (2026-08-30 신설, 2026-09-01 수정)

⚠️ **2026-09-01 수정**: 최초 버전(아래 "휴장일 귀속" 절만 있던 버전)은 거래일에 발행된 뉴스를
**그날 자신의 라벨**에 매칭하도록 구현돼 있었다(`감성분석/taskf_gating.py`의
`pd.merge_asof(..., direction="forward")`가 기본값 `allow_exact_matches=True`를 써서 당일
매칭을 허용). Task F 게이팅 실행 직후 이것이 **누수**임이 지적됐다 — search_backfill/빅카인즈
둘 다 발행 "시각"이 없어 D일 뉴스가 장중에 나왔는지 장 마감 후 그날 결과를 사후 서술한
것인지 구분할 수 없는데, D일 라벨(D일 종가 기준 `excess_return`)에 매칭하면 후자의 경우
"이미 벌어진 일을 사후 서술한 텍스트로 그날 라벨을 맞히는" 구조가 된다. 수정 후 규칙은 다음
하나로 통일된다:

> **뉴스 발행일이 D면, D보다 엄격히 뒤(> D)인 첫 거래일의 라벨에 매칭한다.** D 당일 라벨에는
> 절대 매칭하지 않는다.

이 규칙 하나가 거래일 뉴스와 휴장일 뉴스를 모두 처리한다:
- **D가 거래일**: D+1 거래일(다음 거래일)에 매칭 — D일 라벨(당일 매칭)은 제외
- **D가 휴장일(주말·공휴일)**: 애초에 D 자신이 거래일 후보가 아니므로 "D보다 뒤인 첫 거래일"이
  곧 "그다음 개장일"과 같다 — 기존 "휴장일 뉴스는 다음 거래일에 귀속" 규칙과 결과가 동일하다

구현은 `pd.merge_asof(news, trading_calendar, ..., direction="forward",
allow_exact_matches=False)` — `시장지표/feature_loader.py`의 `_asof_join()`이 쓰는
"published_date < 거래일" 누수방지 관용구(`direction="backward",
allow_exact_matches=False`)와 방향만 반대인 동일 패턴이다.

**이것이 로드맵 Task D "안 B(완화): D-1 09:00~D 09:00 24시간 윈도우"의 실제 구현이다.** 원래
안 B는 "D-1 09:00~D 09:00" 24시간 윈도우로 표현됐지만, search_backfill/빅카인즈에 시각
정보가 없어 그 윈도우를 문자 그대로 계산할 수 없다 — "D일에 발행된 뉴스는 D의 다음 거래일
가격에 반영된다"고 근사한 것이 이 매칭 규칙이고, 그 근사가 안전하려면(미래 정보를 끌어쓰지
않으려면) D 당일 라벨을 반드시 배제해야 한다는 것이 이번 수정의 핵심이다.

**주말 뉴스를 수집·라벨링 대상에서 제외하지 않는다.** 이유:
- 제외하면 전체 뉴스의 상당 비중이 손실된다
- 금요일 장 마감 후 발표되는 실적 등 중요 뉴스를 잃는다
- 월요일에 뉴스가 몰리는 현상은 왜곡이 아니라 실제 정보 흐름을 반영한다 — 월요일 주가는 실제로
  주말 뉴스에 반응한다

거래일 판정은 `daily_labels`(또는 `daily_stock_prices`)에 실제 데이터가 있는 날짜를 기준으로
한다. 수정 전/후 게이팅 결과는 CLAUDE.md D-2 이후 절(또는 세션 기록) 참고 — 누수 제거 후에도
9개 조합 전부 baseline을 유의하게 상회해 신호 자체는 유지됨을 확인했다(다만 격차는 줄었다).

### 보고 항목 (완료 조건)

- 라벨 분포: 방향 5-class / 3-class, 변동성 2-class(임계값 후보 3종 \|z\|>0.5/1.0/1.5 각각)
  전부, 윈도우 N(20/60/120)별로
- **D-8 레짐 특성 3종**(split별로): 전 거래일 평균 \|cr\|, 평균 \|초과수익률\|, KOSPI 평균 \|cr\|
  — 전체 기간을 그대로 쓰므로 test split이 스트레스 기간을 포함한다는 사실이 이 수치로 드러나야
  한다
- 목적 확인: Task A(Task 0)에서 나타난 분포 이동이 해소됐는지

위 항목 출력 후 **정지**. 방향(5/3-class)·변동성(2-class) 임계값 확정은 모두 사람이 한다.

---

## Task F — 비교 실험

### 착수 조건

Task E에서 사람이 임계값을 확정한 후.

### 게이팅 규칙

각 조합에 TF-IDF + 로지스틱 회귀를 먼저 실행한다. 해당 split의 **실제 최빈 클래스** 기준을
유의하게 상회하는 조합에만 KR-FinBERT를 투입한다. **방향 라벨과 변동성 라벨 둘 다 게이팅에
넣어** 어느 쪽에 신호가 있는지 판정한다.

### 모델

`snunlp/KR-FinBert`. **KoBERT는 재실행 금지** — D-1 기준 이미 실패 사례로 확정됐고 보존만
한다(아래 "1차 실험과의 비교 한계" 참고).

### 비교 축

- 라벨: 방향(5-class / 3-class) × 변동성(2-class)
- 윈도우 N: 20 / 60 / 120
- **데이터 소스 교차 평가**(핵심):

| 학습 | 평가 | 목적 |
|---|---|---|
| 크롤링 | 크롤링 | 기준선 |
| 빅카인즈 | 빅카인즈 | 자체 성능 |
| 빅카인즈 | 크롤링 | **대체 가능성 검증** |

→ 3번이 1번에 근접하면 빅카인즈 대체 전략이 성립한다.

- **언론사 집합**: 각자 전체 화이트리스트 / 교집합 통제 — 두 소스의 언론사 화이트리스트가
  서로 달라 성능 차이의 귀속이 애매해지는 것을 통제하기 위해 둘 다 실행한다
- **입력 형식**: 교차 평가 시에는 **제목만 필수**(크롤링 쪽엔 본문이 없으므로 학습-서빙
  불일치를 방지). 빅카인즈 단독 실험에서만 제목+본문 비교를 추가로 수행한다

### 조합1(기존 `sentiment_score`) 처리

**재실행 대상에서 제외한다.** D-1에 따라 이미 확정된 실패 기록을 표에 인용만 하고 다시
학습하지 않는다. 비교표에 아래를 각주로 명시할 것 — 왜 재실행하지 않는지 이유까지 함께 적을 것:

| 설정 | test macro F1 |
|---|---|
| 다수결 baseline | 0.0965 |
| TF-IDF char_wb 2-4gram | 0.1498 |
| KoBERT 1차 학습 | 0.1473 |

### 1차 실험과의 비교 한계 (지시서에 명시할 것)

1차(legacy)는 변동성 2% 초과일만 수집한 선택 편향 구조 + 5-class 절대임계값 라벨 + 244개
조합으로, 신규 실험과 데이터 선택 기준·표본 수·라벨 정의가 모두 다르다.

- **성능 지표(macro F1) 직접 비교 불가**
- 비교 가능한 것: 라벨 분포 안정성, 선택 편향 해소 여부, split별 레짐 특성

### 지표

- macro F1 (주지표 — 클래스 불균형 때문)
- accuracy
- 클래스별 precision / recall
- baseline은 해당 split **실제 최빈 클래스** 기준 (CLAUDE.md 규칙)

### 결정 지점 — 미리 확정하지 말고 표시만 할 것

- 3-class 축 추가 여부(D-9): 5-class 결과 확인 후 사람이 결정
- walk-forward 검증 도입 여부(D-8): Task F 착수 시 사람이 결정

### 완료 조건

위 비교표(데이터 소스 교차 평가 포함) + 지표 출력 후 **정지**.

---

## 공통 설계 원칙

- **데이터 소스 하드코딩 금지.** 라벨링·학습 스크립트 모두 대상 테이블(`daily_news` /
  `daily_news_bigkinds`)을 함수 인자나 설정값으로 받아 두 소스를 같은 파이프라인으로 돌릴 수
  있게 설계할 것
- **학습 기간 하드코딩 금지.** `constants.py`의 `TRAIN_PERIOD_START`/`TRAIN_PERIOD_END`,
  `SPLIT_RATIOS`를 참조할 것 — 날짜·비율을 스크립트에 직접 적지 않는다
- `감성분석/kobert_dataset.py`의 `load_labeled_news()`는 `label_column`/`table_name` 파라미터로
  이미 확장돼 있다(2026-08-30, 뉴스 테이블 자체의 컬럼을 읽는 구조). **위 라벨 저장 방식 변경으로
  이것만으로는 부족하다** — 신규 라벨은 별도 테이블 `daily_labels`에 있으므로 `(ticker, date)`
  조인 로직이 추가로 필요하다(`load_labeled_news()` 확장 또는 신규 함수, 방식은 Task E 구현
  시점에 판단)

---

## 정지 지점 요약

| 단계 | 정지 시 보고 항목 | 사람이 결정할 것 |
|---|---|---|
| E | z 분포, 라벨 분포(방향 5-class/3-class, 변동성 2-class 후보 3종), 레짐 특성 3종 | 방향(5/3-class)·변동성(2-class) 최종 임계값 |
| F | 데이터소스 교차평가 표, 지표(macro F1/accuracy/precision/recall), 게이팅 결과 | 3-class 축 추가 여부(D-9), walk-forward 도입 여부(D-8) |

정지 지점에서 임의 판단으로 다음 단계에 진입하지 않는다.
